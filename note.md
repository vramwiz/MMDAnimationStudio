# MMDアニメーションスタジオ 作業ノート

作業再開時に最初に読む入口。現在位置と直近の作業だけを扱い、詳細は分類別文書を参照する。

## プロジェクトの目的

AviUtl2でPMXモデルを表示し、独立したポーズ・モーション・表情等のオブジェクトから現在フレームの状態を受け取って、MMDのボーン計算とスキニングを行うプラグイン群を開発する。

現在の主要プロジェクトは次の2つ。

- `MMD_Model_Filter`: PMX読込、姿勢合成、CPUスキニング、AviUtl2描画を担当する「モデル表示」。
- `MMD_Pose_Filter`: モデルに依存する姿勢データを編集・保存し、現在フレームの姿勢をモデル表示へ渡す「ポーズ」。

## 現在位置

- PMX 2.0 / 2.1の読込、材質描画、ボーン階層、付与変形、CCD IK、BDEF1 / BDEF2 / BDEF4のCPUスキニングを実装済み。
- ModelとPoseの共通ポーズ編集GUI、D3D11プレビュー、直接ボーン操作、Undo / Redo、左右対称編集、参照画像のコピー・貼り付けと概形合わせを実装済み。
- PoseからModelへの現在フレーム姿勢は、レイヤー別共有メモリとモデルパスハッシュで分離して受け渡す。
- AI MIRAIのNamed Pipe `Aul2MIRAI.v1`から利用するバージョン1 C ABIをModel / Poseから公開済み。
- `get_model_schema`と非破壊の`preview_pose`は実モデルで実機確認済み。
- AI専用診断ビューとして通常、骨格のみ、骨格重畳、単色シルエット、指別ID色、身体のみの6表示を実装し、AI MIRAIから一時表示、画像取得、解除を実機確認済み。
- 2026-08-27に、両腕を頭より高い左右対称V字へ上げた試作姿勢をユーザー確認済みポーズ「万歳」として確定した。移行時点の確定値と評価結果は`D:\DelphiProg\AviUtl2Plugin\MMDAnimationStudio\Learning\MMD_POSE_LEARNING.md`を参照する。
- 「万歳」はAI診断フラグの全6表示で確認し、左右骨格の対称、腕の非交差、診断解除後の状態不変と通常BMP完全一致を確認した。現在の構図では頭頂と手先の一部が上端から切れるため、診断時の一時的な自動フィットが次の改善候補である。
- `sample2.aup2`の起動中モデルへ「万歳」を適用したが、プロジェクトファイル自体は未保存である。再開時は保存状態を再確認する。
- 最新コミット時点でModel / Pose / Aul2MIRAIのWin64 Releaseビルドと実機連携に合格している。
- 2026-08-27に`D:\DelphiProg\test\MMD`から本フォルダへ製品側コードをコピーし、MMD共通コードを`..\AviUtl2PluginLib\MMD`へ分離した。Model / PoseのDebug / Releaseと`MMDAnimationStudio.groupproj`のDebugを、共有側への相対参照だけで警告0・エラー0でビルドした。
- 2026-08-27に`MMDAnimationStudio.dproj`を製品の本命となるAviUtl2拡張プラグインへ変更した。表示名は「MMDアニメーションスタジオ」、プラグイングループは`MMD`。現段階は同名クライアントウィンドウへ空のVCLフォームを表示する最小実装のみで、Win64 Debug / Releaseを警告0・エラー0でビルドし、`C:\ProgramData\aviutl2\Plugin\MMD\MMDAnimationStudio.aux2`を生成済み。
- 2026-08-27に単体`MMDAIPreview`プロジェクトも本フォルダへ移設した。製品側は`Source\AI`、共通処理は`..\AviUtl2PluginLib\MMD`だけを参照し、元のテストフォルダへの参照を持たない。元プロジェクトと重複していたGUIDは新規GUIDへ変更し、`MMDAnimationStudioGroup.groupproj`へ追加した。Win64 Debug / Release、`--self-test`、グループ全体のReleaseビルドに合格済み。
- 2026-08-28に`MMDAIPreview`のポーズ永続化を独自JSONからMMD標準VPDへ変更した。保存先はマイドキュメントの`MMDAnimationStudio\VPD`とし、拡張子は`.vpd`、文字コードはMMD互換のCP932とする。内部処理とNamed Pipeの`pose_data`は引き続き版付きJSONを使い、ファイル境界だけで相互変換する。
- VPD保存先は共有`MmdVpdDirectory`で管理する。プレビューが保存先を取得するときとMMDAnimationStudio本体のプラグイン登録時に存在を確認し、無ければ作成する。パスが同名ファイルで占有されている場合や作成後も存在しない場合はエラーとする。
- 2026-08-27に拡張プラグインのフォームへメインフレームを配置し、「モデル／ポーズ／モーション／表情／設定」の表示切替用ツールバーを追加した。アイコンは`Syncroh2_Extension2`と同じDPI対応線画生成を採用し、現段階では表示のみ。Win64 Debug / Releaseを警告0・エラー0でビルド済み。
- 初回のツールバー実装は、フレームが親パネルへ接続される前のコンストラクタ内で`TToolBar.Images`を設定し、`EInvalidOperation`（親ウィンドウなし）となっていた。参照元と同じくフォームの`ParentWindow`設定後にルートパネルとフレームを生成し、フレームを親へ接続した後の`Show`でアイコンを遅延初期化するよう修正した。単独UIスモークテストとAviUtl2実機プローブで、フォーム、ルートパネル、フレーム、ツールバーがすべて可視状態になることを確認済み。
- ツールバーのホバー／選択色がWindows標準描画では明るすぎたため、参照元と同じ`TToolBarPanelManager`へ切り替えた。通常`$2B2B2B`、ホバー`$B03C3C`、押下`$1F1F1F`、選択`$FF6666`をそのまま使用する。
- ツールバーを左から「PMXの管理／ポーズ・モーション／表情／セリフ／エクスプローラー／音楽／起動」の7項目へ整理した。PMXは人物上半身、ポーズ・モーションは走る人物、表情はスマイル、セリフは吹き出し、エクスプローラーはフォルダー、音楽は音符、起動は緑の再生アイコンとし、各Hintも同じ順序の用途説明へ対応させた。
- 初回表示や横幅縮小でツールバーボタンが折り返された際、親パネルが1行分の固定高のまま下段を隠していた。親パネルを自動サイズ化し、フレームのResizeごとにネイティブボタン矩形の最下端から必要高を再計算してツールバーと親パネルへ反映するよう修正した。幅100pxへ縮めるUIスモークテストで複数行への高さ拡張を確認済み。
- 2026-08-28に共通ポーズ編集GUIの右欄にあった適用、Undo / Redo、各初期化、左右対称、画像合わせを、フォーム上端のアイコンツールバーへ移した。`MMDAnimationStudio`と同じDPI対応線画生成と暗色のホバー／押下／選択色を使う。MMDAIPreview、Model、Pose、拡張プラグインの全編集画面が同じ実装を利用し、96 / 192 DPI、イベント接続、Win64 Debug / Release全体ビルドに合格済み。
- 2026-08-29に表情ページの初期表情を、モーフ名と非ゼロウェイトだけを持つ版付きJSONとして保存する経路を追加した。カタログの各ポーズ項目、D&Dエイリアス、モデル表示の文字列設定`表情`を接続し、描画時はボーン姿勢の後へモーフを適用する。モデル表示の`設定`と初期状態ダブルクリックは同じ共通フォームを使う。モデル表示ではポーズと表情の2アイコン、初期状態編集では目パチと口パクを含む全4アイコンを表示する。
- 2026-08-29に目パチ実装の第1段階として、初期状態編集の目パチページへ単一モーフ一覧と閉眼時モーフ段階を追加した。「なし」は0%、新しいモーフ選択は100%、保存済み設定を開いた場合は保存値を復元する。版付きJSONの`initialEyeBlinkData`をポーズ項目へ保存する。D&D、AviUtl2パラメーター、フレームごとの瞬き処理は次段階とする。
- 2026-08-29に目パチGUIを見出し、単一モーフコンボ、専用トラックバー、間隔・速度・オフセットの数値編集の順へ整理した。リストと段階文字表示は廃止し、時間設定を含む版付きJSONを`initialEyeBlinkData`へ保存する。旧version 1データは既定値を補って読み込む。
- 2026-08-29に口パクGUIの第1段階を追加した。開閉モーフと「あ・い・う・え・お・ん」の音素モーフを個別コンボで選択し、速度と強さを数値編集する。段階GUIは置かず100%固定とするが、各モーフのウェイトは`initialLipSyncData`内に保持する。初回だけ開閉と「あ」を「あ」、各音素を同名モーフへ完全一致で自動割り当てし、その後は明示的な「なし」も保持する初期化フラグを追加した。初期状態カタログの保存と再表示まで接続済み。
- 2026-08-29に口パク実装の第2段階として、モデル表示へ「口パク」グループと「口パクデータ」「口パク速度（秒）」「口パク強さ（%）」を追加した。ポーズのD&Dエイリアスは保存済みJSONを検証して文字列項目へ渡し、同じJSONの速度・強さを数値項目へ展開する。実際の口パク計算は次段階とする。
- 2026-08-29に口パク実装の第3段階として、Syncroh2互換の`Local\ShareTalk`と`Local\GSharedSong`を読み取り専用で参照する処理を追加した。セリフ／ソング参照レイヤーで同じ番号の共有スロットを選び、トークの音量LABは開閉モーフ、トーク／歌の音素は「あ・い・う・え・お・ん」の割り当てモーフへ反映し、口パク速度と強さをオブジェクト別コンテキスト内の補間へ適用する。トーク入力は参考元と同じく歌入力より優先し、共有メモリ不在・不正値・未設定モーフでは描画本体へ影響させない。
- 2026-08-29にモデル表示パラメーターを、モデルファイル、設定、MMD倍率、参照レイヤーグループ（表情・ポーズ・モーション・セリフ・ソング）、目パチグループ、口パクグループ、表示モード、比較用骨格Xずらし、データグループの順へ整理した。ポーズ・表情・目パチ・口パクの文字列項目は末尾のデータグループへ移し、同グループはSDK既定値を閉で登録する。
- 2026-08-29に表情・ポーズ・モーション・セリフ・ソングの参照レイヤーをすべて1～99、初期値1へ統一した。
- 2026-08-29に目パチ実装の第2段階を補完し、モデル表示へ「目パチ」グループと「目パチデータ」「目パチ間隔（秒）」「目パチ速度（秒）」「目パチオフセット（秒）」を追加した。ポーズのD&Dエイリアスは保存済みJSONを検証して文字列項目へ渡し、同じJSONの時間設定を数値項目へ展開する。実際の瞬き計算は次段階とする。
- 2026-08-29に目パチ実装の第3段階を追加した。モデル表示のEffectID別コンテキストへ解析済み設定、解決済みモーフ番号、瞬き進行状態を保持し、オブジェクト相対フレームとFPSから閉眼量を生成する。間隔には参考元と同じ±1秒の揺らぎを持たせるが、オブジェクト固有シードから決定して同一フレーム、巻き戻し、再描画でも結果を固定する。正のオフセットは開始を遅延する。初期表情に同じモーフがある場合は、その値から閉眼時ウェイトへ補間する。
- 2026-08-29にモデル表示の追加実装を責務別に整理した。420行へ増えていたEffectIDコンテキストから口パク設定・モーフ解決・補間を`Context\LipSync`へ分離し、共有メモリ接続と入力文字列解析を`Input\LipSync`、ポーズ入力を`Input\Pose`、目パチ時刻計算を`Runtime\EyeBlink`へ配置した。口パクはLABに加えてセリフの`aiueo`、通常セリフの開閉フォールバック、ソングの`note_lab`／`note_AIUEO`を処理する。追加・移動ユニットには責務、公開API、状態境界、例外時の継続条件をコメントした。
- 2026-08-29に共通編集GUIの追加ユニットも整理した。モーフ一覧・プレビューを`MMD\Editor\Morph`、初期設定フォーム・目パチ・口パクを`MMD\Editor\Setting`へ移し、モーフコンボ解決と数値検証を共通ユニット、モーダル編集入口をダイアログユニットへ分離した。今回追加・拡張したPascalユニットはすべて400行未満とし、公開APIへ呼出側視点の責務コメントを付けた。
- 2026-08-29にPMX管理を完成扱いとした。左右のPMX／ポーズ一覧、人物分類、頭部中心サムネイル、選択・ホバー表示、追加・複製・削除・名称変更・並べ替え、右クリックメニュー、ショートカット、画像コピー／D&D、更新、DPI対応ダーク確認ダイアログを実装した。カタログ項目・索引／個別JSON codec・ポーズD&Dを用途別フォルダへ分離し、PMX管理配下は最大352行、1フォルダ最多4ユニットとした。公開APIコメントを監査し、Win64 Debug / Releaseの警告0・エラー0とUIスモークテストに合格した。
- 2026-08-29にポーズ管理画面の第1段階を追加した。人物分類付きPMX選択ペインを共通Controlへ分離し、PMX管理とポーズ・モーションページで同じ実装を使う。選択中PmxUIDはページ間で追従し、ポーズページ右側にPMX別ポーズ一覧を表示する。データが無いPMXでは空姿勢の「初期状態」を自動登録する。VPD取込と独自グループは次段階とする。Win64 Debug / Releaseを警告0・エラー0でビルドし、2件のPMXを使うUIスモークテストに合格した。
- ポーズページ右一覧のダブルクリックを、PMX管理から開くものと同じ設定フォームのポーズ専用構成へ接続した。ページ切替ツールバーとモーフ等のページは表示せず、「閉じる」またはタイトルバーの×で姿勢JSONを保存して右一覧とサムネイルへ反映する。保存と再読込をUIスモークテストで確認した。Win64 Debug / Releaseのコンパイルは警告0・エラー0。AviUtl2起動中で配置済み`MMDAnimationStudio.aux2`がロックされていたため、今回ビルドの自動配置だけは未完了である。
- ポーズページ右側へPMX管理と同じ5アイコンの操作ツールバー、9項目のポップアップメニュー、Ctrl+C／Delete／F2／Ctrl+上下／F5を接続した。追加、複製、削除、名称変更、上下移動、更新は同じ実装を使い、両画面の差は設定画面と今後実装するD&D入力に限定する。UIスモークテストとWin64 Debug / Releaseコンパイルに合格した。AviUtl2起動中のため自動配置は未完了である。
- `MMD_Pose_Filter`の項目名を`モデルファイル／ポーズ／設定`へ変更した。AIMIRAIで現在選択中の実ポーズオブジェクトが`ポーズ／標準描画`構成であることを確認し、ポーズページ右一覧のD&Dから同じ構成を持つポーズオブジェクトを生成する専用エイリアスへ接続した。PMX管理画面のD&Dは従来のモデル表示生成を維持する。JSON検証とUTF-8 BOMなし出力の単体テスト、UIスモークテスト、拡張とPose FilterのWin64 Debug / Releaseコンパイルに合格した。AviUtl2起動中のため自動配置は未完了である。
- モデル表示のポーズ参照第一段を確定した。画面上の参照レイヤーをSDKレイヤーへ変換し、`GetImageObject`が現在位置でポーズオブジェクトを返した場合だけ、共有メモリのPMXパスを照合して姿勢を適用する。PoseとModelの配置開始位置が異なるため相対フレームは照合しない。オブジェクトが無い位置では共有メモリの残留値を使わず標準姿勢へ戻す。開始位置が異なる成功例、オブジェクト不在、PMX不一致を回帰テストへ追加した。
- ポーズ共有メモリの診断ログはDebugビルド限定とした。`C:\Users\zan12\Documents\MMDAnimationStudio\Logs\PoseSharedMemory.log`へ、モデル側の評価要求・空レイヤー・受信成功／不一致と、ポーズ側の発行成功／失敗を同一形式で記録する。Releaseではログ処理を実行しない。Model / PoseのWin64 Debug / Releaseをビルド・配置し、共有メモリ回帰テストに合格した。
- 2026-08-29に表情管理の第1段階を追加した。機能識別は`Face`とし、表情ページへポーズ管理と同じPMX選択、表情一覧、グループ、5操作ツールバー、右クリックメニュー、ショートカットを接続した。表情はPMX別`Faces`配下へFaceUID単位で保存し、0件なら空モーフJSONを持つ削除不可の「初期状態」を必ず作る。表情を反映した頭部中心サムネイルも生成する。
- 表情一覧のダブルクリックを既存の共通PMX設定フォームへ接続した。表情専用モードではページ切替、ボーン一覧、ポーズ操作を表示せず、既存のモーフ一覧と頭部プレビューだけを使う。閉じるまたは×でFaceUIDのモーフJSONを保存し、一覧とサムネイルへ反映する。
- 表情・目パチ・口パク編集の頭部プレビューは、D3D子ウィンドウ生成後に選択ページ状態を再適用し、「頭」を画面中央へ倍率3.2で表示する。これにより表情ページから直接開いた場合も全身・ボーン表示へ戻らず、モデルだけの読み取り専用プレビューになる。表情モーフ一覧は表示時に左ペインの確定実寸へ展開してから`alClient`へ戻し、高DPIでも一覧下の空白を作らず「すべて解除」を下端へ固定する。「頭」が無ければ「両目」、左右目の中点、首の順に中心を決める。後から生成する「閉じる」ボタンはフォームのDPI調整済みフォントを継承し、文字だけが二重拡大されないようにした。
- `MMD_Face_Filter`を追加した。AviUtl2の`MMD`グループへ`表情`として登録し、`モデルファイル／表情／設定`を持つ。透明画像を返しながらレイヤー別共有メモリへモーフJSONを発行し、モデル表示の既存`表情参照レイヤー`が同一PMXだけを受信する。参照表情は初期表情より優先し、目パチ・口パクはその後へ合成する。空表情による初期表情解除も有効とする。
- `MMD_Face_Filter`のWin64 Debug／Release出力設定を`MMD_Pose_Filter`へ統一し、`.auf2`生成を同じビルドイベントへ揃えた。表情一覧へD&Dを接続し、選択PMXの`モデルファイル`、FaceUIDの`表情`JSON、空の`設定`と標準描画を持つ単一表情オブジェクトをUTF-8 BOMなしエイリアスとして生成する。一時ファイルは画面インスタンスごとに分離し、終了時に破棄する。

## 次の作業

表情管理画面の次段階を扱う。

1. VPD取込の破損・部分失敗表示とPMX別関節可動領域判定を継続する。

### ポーズ管理GUIの完成整理

- 独自グループ、VPDドロップ登録、登録済みVPDの分類横断チェック、一括登録、読み取り専用プレビューまでを現在段階の完成範囲とした。
- 肥大化していた共通テーマを基盤、ボタン、一覧／チェック一覧、コンボへ分割した。Control実装は`AviUtl2PluginLib\MMD\Editor\Theme`へまとめ、基盤テーマは配色とタイトルバーだけを持つ。
- 今回追加・変更した機能ユニットは原則400行未満、同一フォルダのユニット数は最大4件とした。既存D3Dユニットは今回の変更対象外とする。
- 公開APIへ責務、入力、返却内容が分かる日本語コメントを付けた。
- 4プロジェクトのWin64 Debug / Release、DPI 96／192、VPD登録、拡張画面スモークを確認済み。

### 後続課題: PMX別の関節可動領域判定

- 現在のVPD取込は、VPD内のボーン名に対応する移動量とQuaternion回転をポーズへ直接変換する。
- 後続段階で、対象PMXのボーン有無、変形可否、IK、関節角度制限などを参照し、モデル固有の可動領域へ正規化または制限する処理が必要になる。
- 共通VPDライブラリに保存した原本は変更せず、PMX用ポーズを生成または適用する段階で補正する。補正不能なボーンは全体の取込を失敗させず、無視または警告として扱う方針とする。

## 検討中のデータ設計

以下は未実装の検討案。モデル表示が必要とするデータ形式を先に決め、それを基準にポーズとモーションの保存・評価形式を共通化する。

### モデル表示が持つモデル固有データ

- 初期ポーズ。
- 瞬きに使うモーフの割り当て。
- 口パクの「あ・い・う・え・お」等に使うモーフの割り当て。
- 瞬きの速度・間隔、口パクの速度・強さは初期設定JSONで保持し、後続段階でAviUtl2パラメーターへ反映する。

### ポーズとモーションの共通形式

- ボーン移動・Quaternion回転とモーフWeightを、キーフレーム番号付きの共通トラック形式で保持する。
- キーフレーム番号は0以上の整数とする。
- キーフレーム番号0しかないデータをポーズとして扱う。
- 1以上のキーフレームを含むデータをモーションとして扱う。
- モデルの初期ポーズも、キーフレーム番号0だけを持つ同じ形式で表現する。
- ボーンとモーフは名前で永続化し、PMX読込時にモデル内の番号へ解決する案を基本とする。
- 補間方式、重複キーの扱い、欠落チャンネルの意味は未確定。

### 共有メモリ

- 共有メモリはMMDプラグイン群で1個とし、内部をAviUtl2のレイヤー番号に対応する固定スロットへ分ける。
- ポーズ／モーションオブジェクトは、自分が存在するレイヤーのスロットへ現在フレームの評価済みポーズだけを発行する。
- モーション全体は送らない。モーションオブジェクトが前後キーを補間し、そのフレームのボーン姿勢とモーフWeightへ変換してから発行する。
- モデル表示は設定された参照レイヤーのスロットを購読し、自分が受け取るデータを決定する。送信側は特定のモデル表示を宛先指定しない。
- PoseとMotionは共通の評価済みフレーム形式を発行し、モデル表示側は入力元の種類を意識しない。
- 各レイヤースロットは形式バージョン、書込み世代、送信元ObjectID / EffectID、タイムラインフレーム、PMXパスハッシュ、ボーン／モーフ件数、Payloadサイズを持つ案とする。
- 受信側はレイヤー、形式、フレーム、モデルハッシュ、世代、件数、サイズ、書込み完了状態を検証してから適用する。
- 全身224ボーンの評価済み姿勢はFloat32中心のバイナリで約7KB、モーフを含めても概ね8KB程度を想定する。1スロット16KBなら256レイヤーで約4MB、512レイヤーで約8MBとなる。

### 検討が必要な点

- 共有メモリの最大レイヤー数と1スロットの正式なサイズ。
- バイナリレコードのアラインメント、形式バージョン、後方互換規則。
- 同じレイヤーを複数のモデル表示が並列に読む場合の整合性確保。
- オブジェクト削除、時間範囲外、同一フレーム再評価時に古いスロットを受理しない仕組み。
- モデル表示が複数の姿勢・モーションレイヤーを同時に合成するか、初期版では参照先を1レイヤーに限定するか。
- 永続化する全モーションデータと、共有メモリへ発行する評価済み1フレームデータのバイナリ形式を共通化する範囲。

## 直近のポーズGUI更新

- 2026-08-29にポーズ画面へSyncroh2表情画面準拠の独自グループ管理を追加した。グループ数の上限は撤廃し、数字キーは先頭9グループだけに作用する。名称、順序、PoseUID所属順をPMX別`Poses\Groups.json`へ保存する。
- VPDの単体・複数・フォルダドロップを追加した。共通VPDライブラリへVpdUID名でコピーし、元ファイル名、直上フォルダ分類、元パス、内容ハッシュを保存する。同時に選択PMX用PoseUIDへ変換し、分類グループへ登録する。同内容と同一PMXへの再ドロップは重複登録しない。
- 同じグループバーをPMX管理ページの右ポーズ一覧にも接続した。ポーズページとPMX管理ページは同一のPMX別`Groups.json`を使い、ページ切替時に再読込するため、どちらで編集しても相互に反映される。AviUtl2起動中のため実機用`.aux2`は更新せず、別出力先でDebug / Releaseビルドとページ間共有UIテストに合格した。
- 登録済みVPD再利用フォームを追加した。分類コンボの初期値は「すべて」とし、ポーズ名チェックはVpdUIDで保持するため分類切替後も残る。選択行だけを現在PMXで読み取り専用プレビューし、回転・拡大・移動を許可する。閉じるまたは×で選択分を一括登録し、既に現在PMXへ登録済みの項目は無効表示する。PMX管理とポーズページの共通右クリックメニューから開く。
- 再利用フォームの標準VCL配色を共通ポーズ編集テーマへ統一した。分類コンボとチェックリストは暗色背景・明色文字・共通選択色を使い、登録済みで無効な行も本文を暗くしない。タイトルバーを暗色化し、下端を状態表示と右寄せの暗色「閉じる」ボタンに整理した。
- 再利用フォームの暗色Controlが親フォームのフォントを継承せず小さくなっていたため、全Controlへ12ptフォントを継承した。チェック行34px、分類コンボ38px、閉じるボタン116x40px、下端バー64pxへ拡大し、余白と左ペイン幅も合わせて調整した。
- 現在PMXへ登録済みのVPDを無効行にしていたためチェックできなかった。登録済み表示は残して全行をチェック可能とし、同一PMXへの重複は既存のSourceVpdId判定で追加しないようにした。

## 作業時の必須条件

- Delphi 37.0、Win64のみ。Debug / Releaseを警告0・エラー0で確認する。
- AviUtl2の並列呼出しを前提に、共有キャッシュは読取専用、可変状態はEffectIDまたはGUIインスタンス単位で分離する。
- Filter / GUI / C ABIの境界からDelphi例外を漏らさない。
- 毎フレームのファイル再読込、不要な確保、GUI値の書戻しを避ける。
- 1 Pascalユニットは原則400行未満とし、機能追加前に責務分割を検討する。
- ユーザーのモデルファイルをGit管理へ追加しない。
- 形式固有コードは`MMD`配下の`PMX`、`VMD`、`VPD`等へ分け、さらに責務別の下位フォルダへ配置する。命名は左から大分類、小分類、具体的責務の順とし、`PmxCatalogItem`のように関連名が一覧でまとまる形にする。
- PMX/VMD/VPDの形式名は実際に準拠するデータだけへ使い、独自または形式横断データには`Mmd`を使う。
- PMX管理はPmxUID、PMXごとのポーズはPoseUIDで管理し、順序索引と個別データを別JSONへ保存する。旧パス一覧は新構造へ自動移行する。
- 左のPMXを選択すると右にそのPMXのポーズ一覧を表示する。ポーズが未登録なら空ポーズデータの「初期状態」を自動作成し、標準姿勢として表示する。
- 右のポーズをダブルクリックするとPMX管理と同じ設定フォームをポーズ専用構成で開く。上部のページ切替ツールバーとモーフ等の設定ページは作らない。画面下の「閉じる」またはタイトルバーの×で現在の姿勢を採用し、個別JSONの保存後に姿勢データをキーとしてサムネイルを再生成する。
- 右のポーズ一覧はPMX管理と同じ操作ツールバー、ポップアップメニュー、ショートカットを共用する。PMX管理ではモデル表示、ポーズ画面では`モデルファイル／ポーズ／設定`を持つポーズオブジェクトをD&D出力する。

## 文書

- [共通GUIライブラリ](LIBRARIES.md): 再利用可能な独自コントロールの所在、API、使用例、類似部品との使い分け。
- [現在の実装状態](CURRENT_STATE.md): 対応済み機能、データ境界、既知の制限。
- [アーキテクチャ](ARCHITECTURE.md): プラグイン間の責務、状態所有、処理順、ソース構成。
- [ロードマップ](ROADMAP.md): 直近の作業、未対応機能、保留事項。
- [開発履歴](HISTORY.md): 日付付きの実装経過、調査、実機検証。
- [開発・運用ルール](DEVELOPMENT.md): ビルド、配置、コメント、Git運用の詳細。
