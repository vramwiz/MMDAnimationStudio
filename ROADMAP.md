# ロードマップ

## 直近

### セリフ統合（完成）

- 旧`MMD_Serif_Filter`は廃止し、MMD専用`@MMD_Script.obj2`と`MMD_Module.mod2`へ置換済み。AliasはScript基底＋標準描画を生成する。
- 拡張画面のセリフアイコンを維持し、`PanelSerif`への共有フレーム遅延接続、プロジェクト／シーン通知、カーソル移動境界は実装済み。
- Syncroh2互換`Local\ShareTalk`のcodec／PublisherとDLL横断排他を共通化し、MMD Script Moduleから現在フレームを発行済み。
- 配置済み`.mod2`のPublisherから`Local\ShareTalk`、Syncroh2 `SerifDraw`、モデル参照口パクまで終端間テスト済み。MMD側の専用セリフ描画は対象外とし、既存SerifDrawとの互換表示を完成範囲とする。
- `ShareTalkIndex`／`ShareTalkHistory`発行、Syncroh2 `SerifDraw`での字幕表示、新旧セリフ双方の口モーフをAviUtl2実画面で確認済み。
- モデル側は送信元レイヤーを強制評価し、オブジェクト実在とタイムラインフレームを照合する。Scriptの`obj.id`とSDKの`POBJECT_INFO.ID`は別体系なので相互比較しない。
- 接続画面のツールバー管理を`Extension\UI\Navigation`へ分離し、今回追加・変更した機能ユニットを400行未満、同一フォルダ最大4件へ整理済み。

### 表情管理の次段階

- FaceUID別カタログ、初期状態、PMX選択、グループ、一覧操作、表情サムネイルは実装済み。
- 右一覧のダブルクリックは、既存PMX設定フォームの表情ページだけを使う専用構成へ接続済み。
- `MMD_Face_Filter`、モデル表示の表情参照レイヤー接続、表情一覧のD&Dエイリアス生成まで実装済み。

### ポーズ管理の次段階

- 独自グループ、VPDドロップ登録、登録済みVPD再利用GUIまでを現在段階の完成範囲とする。
- 登録済みVPDの分類コンボ、チェックリスト、単一プレビュー、一括登録は実装済み。
- 破損VPD、未知ボーン、部分失敗の処理結果を一括取込後にまとめて表示する。
- PMX別のボーン属性、IK、関節角度制限を使った可動領域判定と補正方式を設計する。

### AI診断表示の次段階

- 現在はモデルパス単位の短時間診断状態であるため、AviUtl2から取得可能な安定IDを調査し、同じPMXを使う複数個体のうち対象だけへ限定できる境界を検討する。
- `body_only`の材質名ヒューリスティックをモデルスキーマへ明示し、材質ごとの身体・衣服分類をモデル固有データとして上書きできるようにする。
- 手のひら法線、指先方向、ローカル軸、関節マーカー、深度、接触・貫通を診断情報へ追加する。

### AI MIRAI全身姿勢

- AviUtl2設定値APIの16,384文字上限を超える姿勢データの保存・受渡し境界を決める。
- 部分姿勢と全身姿勢の正本、永続化、互換性、失敗時の復旧方法を定義する。
- Model / Pose / AI MIRAI間で、巨大JSONを不必要に毎フレーム複製しない。

### 描画同期

- AI MIRAIによる姿勢変更直後のフレーム取得が、更新前の描画を返す競合を整理する。
- 世代番号または状態トークンと、AviUtl2の再評価・再描画完了を対応付ける。
- タイムアウト、オブジェクト破棄、別フレーム移動時の挙動を決める。

## 未対応

- PMD読込とVMDモーション読込。
- SDEF / QDEFスキニング。
- UV、追加UV、材質、インパルスモーフの適用。
- スフィアマップ、Toon、エッジ、物理演算。
- 約262,144展開頂点を超えるモデルの全材質描画。
- モーフ名によるモデル固有の表情、口パク、まばたき割当の正式な保存UI。
- 共通モーションを体格差へ合わせる相対補正。

## 保留・要設計

- 部分ポーズプリセット。
- 参照画像への概形合わせの探索精度向上。
- ポーズ編集の本体メッシュを常時ドラッグ追従させるか、更新頻度を制御するか。
- 表示モデルと複数の外部制御オブジェクトを対応付ける規則。
- 同じボーン／モーフを複数入力が操作する場合の優先順位、上書き／加算、合成順。
- モデル固有データと共通データの解決規則。
- 共有メモリ形式の将来拡張と後方互換規則。
- AviUtl2上の実測負荷に基づく軽量表示の省略範囲。

## 検証原則

- 人工データの単体テストと実PMXの回帰テストを両方用意する。
- Model / PoseのDebug / Release Win64を警告0・エラー0でビルドする。
- AI境界変更時はAul2MIRAIもReleaseビルドし、Named Pipeから往復確認する。
- 実機確認で一時的に姿勢を変更した場合は元状態へ戻し、状態トークンの一致を確認する。
