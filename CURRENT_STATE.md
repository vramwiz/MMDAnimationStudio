# 現在の実装状態

`note.md` から参照する現行仕様の一覧。調査の経緯や日付ごとの検証内容は `HISTORY.md` を参照する。

## プラグイン

### MMDアニメーションスタジオ拡張

- AviUtl2の`MMD`グループへ「MMDアニメーションスタジオ」を登録し、機能切替ツールバーを表示する。
- 人物以外のボーンなしPMXやOBJ等を管理する「アクセサリ」ページをPMX管理の右へ配置した。DPI対応の立方体アイコンを持ち、未登録時だけPMX／OBJドロップ案内と登録結果欄を表示する。1件以上登録すると案内ヘッダーを隠し、操作ツールバーと一覧を上端へ詰める。
- アクセサリ保存層は原本の`SourceUID`と表示項目の`AccessoryUID`を分離する。`Documents\MMDAnimationStudio\Accessories`を保存ルートとし、`SourceIndex.json`、`SourceItems\{SourceUID}.json`、`Sources\{SourceUID}\元ファイル名`、`Index.json`、`Items\{AccessoryUID}.json`へ版付きJSONと原本を保存する。PMX／OBJの同一形式・同一SHA-2ハッシュは重複登録しない。SourceUID別フォルダへPMXの相対テクスチャ、OBJのMTLと相対テクスチャを同居させる。
- アクセサリページ表示中はPMX／OBJの単体、複数ファイル、フォルダをドロップ登録できる。PMXは既存Readerで全内容と描画可能な頂点・面・材質を検証し、ボーン0件の静的PMXとボーンありPMXを共に受理する。OBJは頂点、UV、法線、正負インデックスの面を読み、四角形以上を三角形へ展開する。MTLの`Kd`、`d`、`Tr`、`map_Kd`を材質とテクスチャへ反映する。破損データは登録しない。
- 登録済みPMX／OBJは名称と全体構図の96px静止サムネイル付き一覧に表示し、サムネイルは1件ずつ遅延生成してキャッシュする。カーソル下の1項目だけは100ms間隔、4秒で1回転のYaw回転プレビューに切り替え、離脱やページ切替で静止画へ戻す。上部ツールバーと右クリックメニュからファイル追加、複製、削除、名称変更、上下移動、最新情報を実行でき、Ctrl+C、Delete、F2、Ctrl+上下、F5にも対応する。
- 一覧の選択項目を左ドラッグすると、`アクセサリ`と標準描画を持つBOMなしUTF-8の単一AviUtl2オブジェクトを生成する。`MMD_Accessory_Filter`は`モデルファイル`と`MMD倍率`だけを公開し、拡張子でPMX／OBJ Readerを切り替えて人物用モデル表示と同じ材質・テクスチャ・`draw_poly()`描画経路へ静的メッシュを渡す。ポーズ、モーション、表情、目パチ、口パクは持たない。
- 拡張プラグインへのドロップ通知は表示中ページを自動切替しない。PMX／VPD／VMDはページを維持したまま各Importerへ渡し、Explorer以外で届く未知形式は無視する。ページ切替は上端ツールバーの明示操作だけで行う。
- 終了時のドロップ受付解除は、VCLコントロールに既存の有効なHWNDがある場合だけ`DragAcceptFiles(False)`を呼ぶ。AviUtl2が親HWNDを先に破棄した後で`HandleNeeded`による子ウィンドウ再生成を起こさない。
- アクセサリ管理は保存、取込トランザクション、一覧操作、表示、ホバー回転、ツールバー、メニュー、D&Dを用途別フォルダへ分離済みである。OBJ／MTL Readerは共通MMD側の`MMD\OBJ\IO`に置き、追加範囲は最大311行、1フォルダ直下は最大4ユニットである。
- PMX管理ページは、左に登録PMX、右に選択PMXのポーズを画像付きで表示する。
- PMXとポーズはそれぞれPmxUID、PoseUIDの個別JSONで管理し、索引JSONが表示順を保持する。
- 選択PMXにポーズがない場合は、空の姿勢データを持つ「初期状態」を自動作成して標準姿勢を表示する。
- 右一覧のダブルクリックでPMX管理用の共通設定画面を開く。画面下の「閉じる」またはタイトルバーの×で現在の設定を確定し、個別JSONとサムネイルを更新する。
- 左一覧は人物分類コンボで絞り込み、頭部位置を判定できるPMXは頭を中心にサムネイルを構図調整する。判定できない場合は従来の全身構図を使う。
- 左一覧の右クリックから登録解除、PMXファイルの場所を開く、キャッシュを破棄して最新状態へ更新を実行できる。
- 右一覧はツールバー、右クリックメニュー、ショートカットから追加、複製、削除、名称変更、上下移動、画像コピー、画像D&D、最新状態への更新を実行できる。削除確認はDPI対応のダーク確認ダイアログを使う。
- PMX管理の製品ユニットは責務別フォルダへ分離済みで、各ユニットは400行未満、1フォルダは最大4ユニットである。
- 「ポーズ」ページは、PMX管理と同じ人物分類付きPMX選択ペインと、選択PMXに属するポーズ画像一覧を表示する。左ペインは共通`TPmxCatalogSelector`を使い、ページを移動しても選択中のPmxUIDを維持する。
- ポーズページで選択したPMXにポーズデータが存在しない場合は、既存のPMX別PoseUIDカタログへ空姿勢の「初期状態」を自動作成する。
- ポーズページ右側はPMX管理と同じ5アイコンの操作ツールバー、グループ登録を含む10項目の右クリックメニュー、Ctrl+C／Delete／F2／Ctrl+上下／F5ショートカットを共用する。追加、複製、削除、名称変更、上下移動、最新状態への更新は両画面で同じ動作とする。
- ポーズページとPMX管理ページの右一覧上部は、表情画面と同じ共通グループUIを持つ。グループ数に上限はなく、右クリックまたはF2／Ctrl+上下で作成、名称変更、削除、並び替えを行う。一覧の「グループに登録」で全グループを選択でき、数字キー1～9は先頭9グループ、0は所属解除として働く。両ページは同じPMX別`Poses\Groups.json`を読み書きする。
- VPDファイル、複数ファイル、またはフォルダを画面へドロップすると、フォルダ配下のVPDを再帰列挙する。内容を検証してハッシュで重複排除し、`Documents\MMDAnimationStudio\VPD`の`Sources\{VpdUID}.vpd`、`Items\{VpdUID}.json`、`Index.json`へ共通保管する。元ファイル名をポーズ名、直上フォルダ名を分類名として記録する。
- ドロップしたVPDは同時に選択中PMX用のPoseUIDへ変換し、分類名と同名のグループを作成または再利用して登録する。PoseUIDは参照元VpdUIDを保持するため、同じVPDを同じPMXへ再ドロップしても重複しない。
- ポーズ一覧の右クリック「登録済みVPDから追加...」から再利用フォームを開ける。分類コンボは初期値「すべて」、左はポーズ名チェックリスト、右は選択行だけを現在PMXへ適用する読み取り専用プレビューとする。分類を切り替えてもVpdUID単位のチェックを保持し、閉じる時に選択分を一括登録する。既に現在PMXへ登録済みのVPDは表示で区別するがチェック可能とし、保存時の重複判定で再登録を防ぐ。
- VPD再利用フォームは共通ダークテーマの12pt表示を使う。配色・タイトルバー、ボタン、一覧／チェック一覧、コンボは責務別ユニットへ分離し、他のポーズ編集画面と同じ見た目と操作寸法を保つ。
- 標準VCLのダークテーマ共通化は完成扱い。基盤3ユニットと、`Basic`、`Input`、`Selection`各3ユニットへ責務分離し、全ユニットを400行未満に保つ。`TListView`と独自描画UIはこの完成範囲に含めない。
- 表情ページはポーズページと同じPMX選択ペイン、FaceUID別表情一覧、独自グループ、5操作ツールバー、右クリックメニュー、ショートカットを持つ。表情は各モデルフォルダーの`Faces\Index.json`、`Faces\Items\{FaceUID}.json`、`Faces\Groups.json`へ保存し、項目数が0件なら空の版付きモーフJSONを持つ削除不可の「初期状態」を作る。サムネイルは表情モーフを適用した頭部中心画像とする。右一覧のダブルクリックは共通PMX設定フォームの表情専用モードを開き、ページ切替やボーン操作を出さず、閉じた時点のモーフJSONを保存して一覧へ反映する。選択表情の左ドラッグは`表情 + 標準描画`の単一AviUtl2オブジェクトを生成する。
- 表情サムネイルは材質モーフも反映する。サムネイル形式v2をキャッシュキーへ含め、材質モーフ未対応だったv1画像は自動的に再生成する。
- PMX管理とポーズページのポーズサムネイルはPoseDataとInitialExpressionDataの両方をキャッシュキーと描画入力に使う。PMX設定で表情だけを変更した場合も再生成し、頂点・ボーン・材質等の表情モーフをポーズと同時に反映する。
- 表情、目パチ、口パクの編集プレビューはD3D子ウィンドウ生成後に表情ページ状態を再適用して「頭」を中央表示する。この再適用でモデルだけを表示してボーン編集を無効化する。表情モーフ一覧は高DPIでも固定高を残さず左ペインのクライアント領域全体へ展開し、「すべて解除」を下端へ置く。「頭」が無いPMXでは「両目」、左右目の中点、首の順にフォールバックする。後から生成する「閉じる」ボタンはフォームのDPI調整済みフォントを継承する。
- ポーズページ右一覧のダブルクリックは、PMX管理から開くものと同じ`TMmdModelSettingEditorForm`をポーズ専用構成で開く。ページ切替ツールバーを生成せず、モーフ等の設定ページを出さずにボーン編集だけを表示する。「閉じる」またはタイトルバーの×で姿勢JSONをPoseUID別ファイルへ保存し、右一覧を再読込してサムネイルを再生成する。
- ポーズページ右一覧からのD&Dは、選択PMXの`モデルファイル`、選択姿勢の`ポーズデータ`と標準描画を持つ`MMDポーズ@MMD_Script`オブジェクトを生成する。PMX管理画面のD&Dは従来どおりモデル表示オブジェクトを生成し、両画面で出力を分ける。
- 「モーション」ページを「ポーズ」の右へ独立追加した。左は共通PMX選択、右は選択PMX用MotionUID一覧とし、ページ間でPmxUIDを同期する。モーションには空の「初期状態」を作らず、未登録時は0件表示とする。
- VMD単体、複数ファイル、フォルダのドロップを受け付け、VMD 0002を検証して内容ハッシュで重複排除する。原本は`Documents\MMDAnimationStudio\VMD\Sources\{VmdUID}.vmd`へコピーし、VmdUID別メタデータと索引を保存する。同じVMDを同じPMXへ再登録しても重複しない。
- PMX別モーションは`Motions\Items\{MotionUID}.json`と`Motions\Index.json`へ保存する。右一覧ではVMD各ボーン／モーフトラックの先頭キーをPMXへ適用した静止サムネイルを表示し、追加、複製、削除、名称変更、上下移動、更新をポーズ由来のアイコンと操作体系で提供する。
- VMD取込時に全ボーン／モーフキーと4系統のBezier制御点を内部データへ変換する。変更しないVmdUID正本は`VMD\Data\{VmdUID}.json`、PMX別に編集可能なコピーは`Motions\Data\{MotionUID}.json`へ版付きJSONとして保存する。一覧読込時はメタデータだけを読み、実データは必要時にデシリアライズする。
- MotionUIDの複製は実データも別ファイルへコピーするため、複製後の編集は互いに独立する。旧項目に実データが無い場合は、保存済みVmdUID正本またはVMD原本から自動生成する。
- モーション一覧はカーソル下の1項目だけVMD原本を読み込み、30fps基準・100ms更新でボーン移動、Quaternion回転、モーフを補間してサムネイル内再生する。カーソル離脱、ページ非表示、別項目への移動時は再生状態を破棄し、静止サムネイルへ戻す。
- ホバー再生はボーン／モーフの全キーを使うが、一覧側のキー間補間は移動・モーフの線形補間とQuaternion Slerpであり、VMD固有Bezier曲線はまだ適用しない。Script Module側の現在フレーム評価では保存済みBezierを使用する。モーション編集は未実装とする。
- PoseとMotionは透明図形＋Filterを使わず、既存`@MMD_Script.obj2`へ追加した`MMDポーズ`／`MMDモーション`Scriptオブジェクトと`MMD_Module.mod2`を使う。Moduleは`set_pose`／`set_motion`を公開し、Scriptの1始まりレイヤーをSDKの0始まりへ変換して既存の専用共有メモリへ発行する。
- Motion ModuleはScriptの`obj.id`由来のキーでデシリアライズ結果をキャッシュし、X／Y／Z移動はVMD Bezier、回転はBezier適用率によるQuaternion Slerp、モーフは線形補間する。共有スナップショットはPMXパスハッシュ、ObjectID／EffectID、タイムラインフレーム、モーションフレーム、名前付きボーン姿勢とモーフウェイトを持つ版付きバイナリ形式である。
- モーション一覧の選択項目を左ドラッグすると、選択PMXの`モデルファイル`とMotionUIDの版付き`モーションデータ`、標準描画を持つ`MMDモーション@MMD_Script`オブジェクトを生成する。D&D開始時だけ実データをデシリアライズして検証・再シリアライズし、オブジェクト長は最終キーを含む`MaxFrame + 1`とする。
- 旧`MMD_Pose_Filter`／`MMD_Motion_Filter`は通常グループビルドと配布対象から外し、更新インストール時に既存`.auf2`を削除する。

### 表情

- AviUtl2上のグループ名は`MMD`、プラグイン名は`表情`、プロジェクト名は`MMD_Face_Filter`。
- 設定項目は`モデルファイル／表情／設定`。設定ボタンは表情ページと同じ表情専用モーフ画面を開く。
- 描画時は透明な1x1画像を出力しながら、PMXパスハッシュと版付きモーフJSONをレイヤー別の専用共有メモリへ発行する。
- モデル表示は`表情参照レイヤー`の画像オブジェクトが現在位置に存在し、PMXパスが一致した場合だけ表情を受信する。参照表情はモデル表示内の初期表情より優先し、その後へ目パチと口パクを合成する。空の表情JSONも有効値として初期表情を0%へ解除する。
- `MMD_Face_Filter.dproj`はWin64 Debug／Releaseのビルド後に`MMD_Face_Filter.auf2`を生成する。

### セリフ入力

- セリフは図形オブジェクトへ付けるFilterではなく、MMD専用Scriptオブジェクトとして生成する。基底エフェクト名は`MMDセリフ@MMD_Script`、プロジェクトは`MMD_Serif_Module`である。
- `@MMD_Script.obj2`が`キャラクター／感情／演出／セリフ／母音／LAB／UID`を保持し、`MMD_Module.mod2`の`set_text`をSyncroh2互換の12引数で呼ぶ。UIDは編集・照合用に保持し、共有メモリへ出す実行時UIDは評価ごとに生成する。
- Alias ProviderはScriptを`[0.0]`、標準描画を`[0.1]`として直列化する。図形および旧`MMD_Serif_Filter`は生成・配置しない。
- Script評価時に相対／タイムラインフレーム、FPS、総時間、7項目からSyncroh2互換キー文字列を生成し、Scriptの1始まりレイヤーをそのまま`Local\ShareTalk`へ発行する。
- `ShareTalk`のMMD送受信は共通の名前付きWindows Mutexを使う。配置済みModuleから別EXEへの発行・読取テストでDLL境界を確認済み。
- Syncroh2の`SerifDraw`に対応するため、本文と同時に`Local\ShareTalkIndex`と`Local\ShareTalkHistory`も共通`SerifSharedIndex`形式で発行する。配置済みMMDセリフDLLの出力をSyncroh2の実`TSerifDrawReceiver`が受理する終端間テストで確認済み。
- 配置済みModuleからモデル入力までの終端間テストと実画面確認で、送信元オブジェクトの実在、タイムラインフレーム、LAB／母音の受信を確認済み。Script側IDとSDK側IDは別体系なので一致条件に使わない。
- `MMD_Serif_Draw_Filter.dproj`が共通SerifDraw本体を参照し、`MMDセリフ表示`として`Plugin\MMD`へ配置する。Syncroh2版とは公開名、グループ名、ファイル名、配置先を分離する。
- セリフページ選択時に共有`TFrameSerif`をHost Bootstrap経由で遅延生成する。MMD専用HostがAviUtl2のプロジェクト読込／保存とシーン変更をVCLタイマー境界で同期し、共通Serif自身のカーソル移動を利用する。
- MMD Profileは表示エフェクト名`MMDセリフ表示`を登録し、共有フレームの表示Alias／ボード機能からMMD版表示フィルターを生成する。

### モデル表示

- AviUtl2上のグループ名は `MMD`、プラグイン名は `モデル表示`。
- `モデルファイル`でPMXを指定し、`MMD倍率`でPMX座標からAviUtl2空間への内部縮尺を調整する。初期値は15倍。
- `設定`でPMX管理の初期状態ダブルクリックと同じ共通フォームを開き、ポーズと表情を編集する。モデル表示から開いた場合はポーズと表情の2アイコンだけ、初期状態のダブルクリックではポーズ、表情、目パチ、口パクの全アイコンを表示する。
- モデル表示の設定画面はモードレスで開き、設定ボタンコールバックを直ちにAviUtl2へ返す。表情モーフを変更すると、設定画面内D3Dプレビューと同時に配置済みAviUtl2オブジェクトの`表情`JSONへ書き戻し、ホスト側も画面を開いたまま再描画できる。コールバック引数の`PEDIT_SECTION`は復帰後に参照せず、呼出中にコピーした`SetObjectItemValue`関数だけを保持する。連続操作は既存の約30fps更新へ集約し、画面を閉じた際にもポーズと表情の最終値を確定する。同時に開けるモデル設定画面は1つとする。
- PMX管理のポーズ一覧から開く初期状態設定では、表情からポーズ・目パチ・口パクへ移動しても編集済み表情を保持する。画面表示のためのモーフウェイト設定は編集通知を発生させず、D3Dプレビューだけを明示的に更新する。
- PMX管理のポーズ編集を確定するとポーズ用サムネイルキャッシュ領域を明示的に削除し、一覧を再読込して、保存後のポーズと初期表情からPNGを再生成する。
- `表情`は版付きJSON文字列としてモデル表示オブジェクトへ保存し、モーフ名と0より大きいウェイトだけを保持する。描画時は標準／外部ポーズの後へ頂点・ボーン・グループ・フリップモーフを適用する。
- 参照レイヤーはAviUtl2画面上の1始まりで、表情・ポーズ・モーション・セリフ・ソングの全項目を1～99の範囲とする。
- モデル表示パラメーターは、モデルファイル、設定、MMD倍率、参照レイヤー（表情・ポーズ・モーション・セリフ・ソング）、目パチ、口パク、表示モード、比較用骨格Xずらし、データの順に登録する。表情・ポーズ・モーション参照は各専用共有メモリへ接続済みである。セリフとソングは口パク共有メモリの1～99の参照スロットとして使用する。末尾のデータグループは既定で閉として登録するが、AviUtl2側が閉状態を反映しない版も想定する。
- 外部ポーズが無効、時間範囲外、別モデル、別フレーム、不正データの場合は履歴を使わず標準姿勢へ戻す。
- モデル表示の`ポーズ参照レイヤー`は画面上の1始まりで指定し、内部では1を引いたSDKレイヤーを現在位置で`GetImageObject`する。対象オブジェクトが存在する場合だけ、同じSDKレイヤーとPMXパスハッシュの共有スナップショットを版付き姿勢JSONとして解析し、標準姿勢の後へ同名ボーン単位で上書きする。PoseとModelの配置開始位置が異なっても適用できるよう、オブジェクト相対フレームは照合しない。
- `モーション参照レイヤー`も`GetImageObject`でMotion Scriptを先に評価し、PMXパスとタイムラインフレームが一致するスナップショットだけを受理する。ボーンは標準姿勢の後、外部Poseの前へ適用する。モーフはMotionを土台に表情を上書きし、その後へ目パチ・口パクを合成する。Motionが目またはリップ表示枠を操作する間は自動目パチ／口パクを二重適用しない。
- `セリフ参照レイヤー`も画面上の1始まりで指定し、`GetImageObject`で送信元セリフを先に評価する。送信元オブジェクトが実在し、`ShareTalk`に空でない`source_object`があり、`current_frame`がモデルの現在タイムラインフレームと一致する場合だけ口パクへ適用する。Scriptの`obj.id`とSDKの`POBJECT_INFO.ID`は別体系なので比較せず、セリフ範囲外の残留値は受理しない。

### ポーズ

- PMX参照とシリアライズ済みの`ポーズデータ`を`MMDポーズ@MMD_Script`オブジェクト自身に保持する。AviUtl2上の編集入口は現段階では持たない。
- 姿勢JSONはバージョン、ボーン名、移動、Quaternion回転を持つ。
- 未知バージョン、壊れたJSON、空の名前、非数・無限値、長さ0のQuaternionを拒否する。
- 設定GUIのOK時だけ対象オブジェクトへ保存し、キャンセル時は変更しない。

## PMXと姿勢計算

- 通常の付与回転・移動は付与元のユーザー変形、IK、多重付与後のローカル量を合成する。付与元と対象が親回転を共有する捩り分散骨でも、共通の親回転を二重適用しない。
- PMX 2.0 / 2.1のヘッダ、モデル情報、頂点、面、テクスチャ、材質、ボーン、モーフを解析する。
- モデル解析結果は絶対パス単位の読取専用キャッシュとして共有する。
- BDEF1 / BDEF2 / BDEF4をCPU線形スキニングする。SDEF / QDEFの変形計算は未対応。
- 親子変換、ローカル／グローバル付与変形、CCD IKとリンク角度制限を計算する。
- グループ、頂点、ボーン、材質、フリップモーフをプレビューとモデル描画へ適用できる。材質モーフは全PMXオフセットを保持し、現在使用するDiffuseとSpecularStrengthを乗算／加算する。材質Index -1による全材質指定にも対応する。
- UV、追加UV、インパルスモーフは解析のみで、変形への適用は未対応。材質モーフのAmbient、Edge、Texture／Sphere／Toon tintは保持するが、対応する描画機能が未実装のため現時点では描画へ反映しない。

## AviUtl2描画

- PMXのインデックスを材質単位の非インデックス三角形へ展開し、`draw_poly()`へ渡す。
- MMDのY上向きをAviUtl2へ合わせてYを反転し、三角形順も交換する。
- 低ポリゴンモデルは全材質をPMX順に両面描画する。
- 約262,144展開頂点を超えるモデルは描画キュー上限を避けるため材質選別へフォールバックする。ふらすこ式風きりたんの固定優先枠は左右髪、手、腕、脇と「きりたん砲消」対象の`weapon`を含む。描画時は解決済みAlphaで実際に表示される頂点だけを数え、材質モーフによって透明から表示へ変わった袖・衣装等を空き枠へ追加する。これにより複数の非表示切替で空いた枠も新しい表示材質へ利用する。
- 頂点カラー材質にもAviUtl2が要求する1×1白画像リソースを渡す。
- スフィアマップ、Toon、エッジ、テクスチャへのDiffuse RGB乗算は未対応。

## ポーズ編集GUI

- D3D11でCPUスキニング済みPMX、通常テクスチャ、簡易照明、骨格を表示する。
- 右ドラッグでカメラ回転、空白左ドラッグでパン、ホイールでズームする。
- A / S / Dで正背面、左右、上下の固定視点を切り替える。
- ボーンまたは関節の左ドラッグで姿勢を編集する。X / Y / C / Zでローカル軸制限、Gで5度スナップする。
- PMXの固定軸とローカル座標を保持し、腕捩り等の補助ボーンはモデル指定の軸へ沿って操作する。選択・ホバー中のボーンは色付き骨格線で示し、不透明な立体形状でモデルを覆わない。
- Lでボーン固定、左右対称編集、選択枝の初期化、最大100操作のUndo / Redoに対応する。
- 適用、Undo / Redo、ボーン・枝・全体の初期化、左右対称、画像への概形合わせは、共通編集フォーム上端のDPI対応アイコンツールバーから操作する。数値入力と確定／キャンセルは右欄に残す。
- Ctrl+Cで骨格なし画像をClipboardへコピーし、Ctrl+Vで参照画像を半透明表示する。
- `画像へ概形合わせ`は主要ボーンを段階探索する。現在は追加調整を保留している。
- 表情ページは共通の仮想モーフ一覧を使い、PMXの眉、目、リップ、その他、表示・アクセサリ、未分類を見出し付きで表示する。材質モーフはPMX表示枠にかかわらず「表示・アクセサリ」へ分離し、0／100%の二値操作に固定する。それ以外のウェイトは連続操作でき、左右端へ吸着する。
- 表情ページの非ゼロモーフ値は、ポーズ項目の`initialExpressionData`とD&Dエイリアス内のモデル表示`表情`へ保存する。目パチページは見出し、「なし」を先頭に持つ単一モーフコンボ、閉眼時ウェイトの専用トラックバー、間隔・速度・オフセットの数値編集を表示し、すべてを`initialEyeBlinkData`へ保存・復元する。「なし」は0%固定、ユーザーが別モーフを選択した直後は100%とする。D&D時はモデル表示の「目パチ」グループへ「目パチデータ」、間隔、速度、オフセットを渡す。モデル表示はEffectIDごとのコンテキストで目パチ設定と進行状態を保持し、間隔±1秒の安定した揺らぎ、閉眼・開眼カーブ、巻き戻し、同一フレーム再評価に対応する。正のオフセットは瞬き開始を遅らせる。プレビュー更新は約30fpsへ集約する。
- 口パクページは見出しの下に開閉、あ・い・う・え・お・んの各モーフコンボ、速度、強さを表示する。各コンボは「なし」を持ち、選択済みモーフの段階はGUIでは100%固定、内部データには個別ウェイトを保持する。未設定データを初めて開いたときは、開閉と「あ」に「あ」、残る音素に同名モーフを完全一致で自動割り当てし、存在しないものは「なし」にする。初回確定後は明示的な「なし」も保存して自動割り当てを繰り返さない。すべてを`initialLipSyncData`の版付きJSONとしてポーズ項目へ保存・復元する。D&D時はモデル表示の「口パク」グループへ「口パクデータ」、速度、強さを渡す。描画時はSyncroh2互換の`Local\ShareTalk`と`Local\GSharedSong`から設定された参照レイヤーを読み取り、音量LABを開閉モーフ、音素LAB、セリフの`aiueo`、歌の`note_lab`／`note_AIUEO`を対応するモーフへ反映する。音素情報がない通常セリフは速度に従う開閉口パクへフォールバックする。共有データは読み取り専用で、入力不在・不正時は通常描画を継続する。
- 高DPI表示に対応し、96 DPIと192 DPIの配置テストを持つ。

## プラグイン間通信

- Pose Scriptは`GetImageObject`による現在フレーム評価時にModuleの`set_pose`を呼び、レイヤー別共有メモリへ姿勢を発行する。
- 共有領域は形式バージョン、世代、ObjectID / EffectID、PMXパスハッシュ、フレーム、UTF-8姿勢JSONを持つ。
- 名前付きMutexを使い、異なるレイヤー、モデル、フレームのデータを受理しない。
- Modelの可変状態はEffectID別コンテキストで管理し、同一PMXを使う複数オブジェクト間で混在させない。
- Debugビルドだけ、送受信診断を`C:\Users\zan12\Documents\MMDAnimationStudio\Logs\PoseSharedMemory.log`へUTF-8で記録する。Model側は`evaluate_request`、`layer_empty`、`receive_ok`、`receive_miss`、Pose側は`publish_ok`、`publish_failed`を、時刻、PID、SDKレイヤー、フレーム、ObjectID / EffectID、PMXハッシュ、JSONバイト数とともに出力する。Releaseビルドではログ処理を実行しない。

## AI MIRAI連携

- 外部AIの入口はAI MIRAIのNamed Pipe `Aul2MIRAI.v1`へ統一し、MMD独自Pipeは持たない。
- Model / PoseはDelphiランタイム非依存のUTF-8 JSONを扱うバージョン1 C ABIを公開する。
- `get_model_schema`はモデル識別情報、全ボーン、親子関係、変形階層、フラグ、IK情報を返す。
- `preview_pose`は`replace` / `merge`、移動、QuaternionまたはEuler度数を受け、姿勢を正規化して最終ワールド位置を返す。
- 重複ボーン、未知ボーン、不正な数値は拒否する。
- 非破壊プレビュー後に元の`state_token`へ戻ることを実機確認済み。
- AI確認専用の一時診断表示として、`normal`、`bones`、`bone_overlay`、`silhouette`、`finger_id`、`body_only`を提供する。
- 診断表示はモデルパス単位の排他状態として最長15秒だけ保持し、AI MIRAIが画像取得後にトークンを使って必ず解除する。プロジェクト設定とUndo履歴は変更しない。
- `finger_id`は衣服を除外した身体材質上で、各頂点へ影響する指ボーンを親指=赤、人差指=黄、中指=緑、薬指=青、小指=紫として表示する。右側は65%の明度で区別する。
- `body_only`は材質名から身体を推定する補助表示であり、モデルによって身体の欠落や誤分類があり得る。完成判定には通常表示を併用する。
- 診断表示は現在モデルパス単位である。同じPMXを使う複数のモデル表示が同一フレームに存在する場合、その全個体へ一時的に同じ診断モードが適用される。
- `MMDAIPreview`が確定候補とGUI微調整を保存するファイルはMMD標準VPDとし、`Documents\MMDAnimationStudio\VPD`へCP932で保存する。内部姿勢とNamed Pipeは版付きJSONのまま維持し、ファイル入出力時だけVPDへ相互変換する。
