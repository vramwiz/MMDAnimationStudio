# MMD プラグイン開発履歴

2026-08-24から2026-08-26まで `note.md` に記録していた設計判断、実装経過、検証結果の保存版。

この文書は旧ノートを情報保全のためそのまま収録している。文中の「現在」「次の作業」「未対応」は記録当時の状態であり、現行の指示ではない。現在の状態は [CURRENT_STATE.md](CURRENT_STATE.md)、次の作業は [note.md](note.md) と [ROADMAP.md](ROADMAP.md)、恒久的な設計は [ARCHITECTURE.md](ARCHITECTURE.md) を参照する。

## 2026-08-29 モーフ設定GUIの責務分割

- 完成したモーフ設定GUIを開発ルールに沿って整理した。545行だった`MmdMorphSettingList`から、PMX分類行構築を`MmdMorphSettingRows`、行描画を`MmdMorphSettingListRenderer`、端吸着と連続値／2値規則を`MmdMorphSettingValue`へ分離し、入力と状態管理を397行へ縮小した。
- 各ユニット先頭と公開APIへ責務、入出力、通知のコメントを追加し、共通GUI文書と現在状態を更新した。
- 全4プロジェクトのWin64 Debug / Releaseを警告0・エラー0でビルドし、モーフ操作回帰と96 / 192 DPI配置テストに合格した。

## 2026-08-29 初期表情の保存とモデル表示への適用

- モデル表示へ文字列設定`表情`を追加し、形式を`{"version":1,"morphs":[{"name":"...","weight":...}]}`とした。モーフは番号ではなく名前で解決し、0より大きい値だけを保存する。重複名、未知バージョン、非数、無限値、0～1外の値、16,384文字超過は拒否する。
- PMXカタログのポーズ項目へ`initialExpressionData`を追加し、表情ページの値を個別JSONへ保存する。モデル表示の`設定`ボタンと初期状態ダブルクリックは同じ共通フォームを使う。モデル表示ではポーズと表情だけ、初期状態編集では目パチと口パクを含む全ページを表示する。
- 目パチ実装の第1段階として、初期状態編集へ単一モーフ選択と閉眼時ウェイトを追加し、`initialEyeBlinkData`として保存・復元する。D&Dとモデル表示パラメーター、瞬き評価は後続段階へ分離する。
- D&Dエイリアスへ`表情`を含め、AviUtl2のモデル表示では標準ポーズと外部ポーズを合成した後に、頂点・ボーン・グループ・フリップモーフを適用してからCPUスキニングする。

## 2026-08-27 AI専用診断ビュー

- AI MIRAIから一時的な診断描画を要求するC ABI操作を追加した。開始と解除は、C ABIの必要サイズ取得と本書込みによる二重呼出しに対して冪等にした。
- 診断状態はモデルパス、表示種別、解除トークン、有効期限だけをモデルDLL内へ保持し、15秒で失効する。ポーズやAviUtl2設定値、Undo履歴は変更しない。
- `normal`、`bones`、`bone_overlay`、`silhouette`、`finger_id`、`body_only`の6表示を実装した。骨格はモデル本人基準で左=青、右=赤、中央=黄とした。
- `finger_id`は身体材質だけを表示し、頂点へ影響する指ボーンのうち最大ウェイトを親指=赤、人差指=黄、中指=緑、薬指=青、小指=紫へ割り当てた。右手は明度65%とした。
- `body_only`は材質名から身体を推定するヒューリスティック表示とした。衣服下に身体メッシュがないモデルや材質名が標準的でないモデルでは不完全になるため、通常表示との併用を必須とする。
- 大型モデルで診断用の全材質描画がAviUtl2の頂点キュー上限を超え、単色シルエットの一部が欠ける問題を実機で発見した。通常表示と診断表示で材質選別を共通化し、全身シルエットへ修正した。
- AviUtl2 v2.1.3a、`sample2.aup2`、ふらすこ式風きりたんで6表示をNamed Pipeから取得した。各応答の`diagnostic_view_released=true`、取得前後の`state_token`一致、診断解除後の通常BMPが診断前とSHA-256一致することを確認した。
- Model / Pose / Aul2MIRAIのWin64 Debug / Releaseを警告0・エラー0でビルドし、Release配置から`finger_id`取得と通常表示復帰を再確認した。

## 構成方針

- MMD関連で複数のプラグインが必要になることを前提とする。
- Delphiプロジェクトファイルはルートへ `MMD_xxx` の名前で配置する。
- 共通ライブラリは `Source\Lib`、各プラグイン固有コードは `Source\Plugin` に分ける。
- AviUtl2上のグループ名は `MMD` とする。
- AviUtl2上のプラグイン名には `MMD` を付けない。
- Filter設定項目は `Source\Lib\FilterTable\PluginFilterTable.pas` で順次登録する。
- MMD共通部は`Core`（モデル・姿勢計算）、`IO`（PMX・JSON）、`IPC`（共有メモリ）、`Editor`（GUI）、`Editor\D3D`（プレビュー）へ分ける。Plugin固有部も`Context`、`Input`、`Render`、`Editor`へ分け、同一フォルダと統括ユニットへの機能集積を避ける。
- 1ユニットは原則400行未満とし、機能追加で超える前に責務単位で分割する。

## 現在の開発段階

`MMD_Model_Filter`でPMX表示とCPUスキニングを確認し、独立した`MMD_Pose_Filter`から現在フレームの姿勢を参照する段階。`draw_poly()`を利用する描画経路の事前検証は完了した。

- AviUtl2上のプラグイン名は `モデル表示`、グループ名は `MMD`。
- `モデルファイル` で現在はPMXを選択できる。PMD対応は今後追加する。
- PMX 2.0 / 2.1のヘッダ、モデル情報、頂点、面、テクスチャ、材質を読み込む解析器を実装済み。
- 解析済みモデルは絶対パス単位で共有・読取専用キャッシュに保持し、フレームごとのファイル再読込を行わない。
- PMXのインデックスを材質単位で非インデックス三角形へ展開し、通常テクスチャ付き／材質色付きの法線頂点として`draw_poly()`へ渡す。
- 描画用頂点配列はレンダースレッドごとに保持し、同じ材質頂点数で再利用する。
- MMDのY上向きをAviUtl2の画面上方向へ合わせるためYを反転し、その反転による表裏の逆転を三角形頂点順の交換で補正する。
- `MMD倍率`でPMX座標からAviUtl2空間への内部縮尺を0.1～100.0倍で調整する。初期値は15倍。標準のオブジェクト拡大率とは分離し、将来は頂点、ボーン移動、モーション移動へ共通適用する。
- PMXボーンの名前、絶対位置、親番号、フラグを解析する。ボーン一覧のDebugログ出力は実機確認完了後に削除した。
- PMX頂点の変形方式（BDEF1 / BDEF2 / BDEF4 / SDEF / QDEF）、最大4本のボーン番号とウェイト、SDEFのC / R0 / R1を保持する。ボーン読込後に番号、ウェイト範囲、ウェイト合計を検証する。
- 描画方式から独立した`PmxPose`で、ボーンのローカル移動、Quaternion回転、親子階層のグローバル姿勢、BDEF1 / BDEF2 / BDEF4のCPU線形スキニングを計算する。SDEF / QDEFの変形計算は未対応で、現時点では元頂点を維持する。
- モデル表示側へ置いた検証用の`ボーン名`と`ボーン回転X / Y / Z`は任意ボーンの実機確認完了後に削除し、姿勢の所有を`MMD_Pose_Filter`へ移した。
- `MMD_Pose_Filter`の`姿勢データ`は`{"version":1,"bones":[{"name":"左腕","translation":[0,0,0],"rotation":[0,0,0,1]}]}`形式のJSONで、複数ボーンの移動とQuaternion回転をポーズオブジェクトへ保存する。未知バージョン、壊れたJSON、空のボーン名、非数・無限値、長さ0のQuaternionは拒否する。モデル表示側は解析結果をEffectID別コンテキストへ保持する。
- モデル表示側の`ポーズ参照レイヤー`はAviUtl2画面上の1始まりのレイヤー番号を指定し、0を参照なしとする。`GetImageObject`で現在フレームのポーズ評価を発生させ、ポーズ側がレイヤー別共有メモリへ発行した姿勢をモデル側が受信する。PMX絶対パスのハッシュとタイムラインフレームが一致する場合だけ適用し、参照なし、時間範囲外、別モデル、不正データの場合は履歴を使わず設定済み標準姿勢へ戻す。
- 共有メモリはSyncroh2の`CreateFileMapping`方式を踏襲し、レイヤーごとの固定長領域、名前付きMutex、形式バージョン、世代番号、送信元ObjectID / EffectID、PMXパスハッシュ、タイムラインフレーム、UTF-8姿勢JSONを持つ。異なるレイヤー、モデル、フレームのデータを受理しない。
- 表示モデルの可変状態はAviUtl2が通知するEffectIDごとのコンテキストで管理し、ObjectIDも記録する。`Func_Create` / `Func_Destroy`を寿命境界として使うため、同一PMXを参照する複数の表示モデルでも標準姿勢と外部姿勢の解析キャッシュは混在しない。
- `標準ポーズ設定`ボタンは専用のモーダルGUIを開く。左にボーン一覧、中央に姿勢反映済み骨格プレビュー、右に選択ボーンの移動XYZと回転XYZを置き、OK時だけ`標準姿勢データ`JSONをモデル表示オブジェクトへ保存する。キャンセル時はAviUtl2設定を変更しない。外部ポーズは標準姿勢へ上書き合成する。
- 2026-08-24: 表示モデルの標準ポーズ編集GUIについて実機動作を確認した。次段階として同じGUIを共通化し、`ポーズ`側にも`ポーズ設定`ボタンを追加した。こちらはOK時に対象ポーズオブジェクト自身の`姿勢データ`へ保存し、モデルの`標準姿勢データ`とは混在させない。
- 2026-08-24: 標準ポーズとポーズの両編集GUIについて実機動作を確認した。骨格だけだった中央プレビューをD3D11ビューポートへ置き換え、CPUスキニング済みPMXポリゴンを材質色と簡易照明で表示し、その上へ選択ボーンを橙色、その他の骨格を水色で重ねる。D3DのDevice、SwapChain、深度・頂点バッファはGUIインスタンスごとに所有し、複数モデル編集時に状態を共有しない。初期D3D段階ではテクスチャ、カメラ操作、ボーンの直接ドラッグは未対応。
- 2026-08-24: 肥大化防止のためソースを用途別フォルダへ再配置した。562行だったPMX読込は、バイト列、頂点・面、材質、ボーン、統括・キャッシュへ分割した。514行だったモデルFilterは、入力、描画、標準姿勢ボタン、Context、フレーム統括へ分割した。520行だったD3Dプレビューは、シーン頂点生成、D3D資源・描画、VCLコントロールへ分割した。現在の全Pascalユニットは400行未満。
- 2026-08-24: 独自設定画面だけモデルが細く表示された原因は、縦長ビューポートでもX/Yへ同じNDC倍率を適用し、実ピクセル上の横縮尺が小さくなっていたことだった。ビューポートのピクセル幅・高さから共通PixelScaleを求め、X/Y別のNDC倍率へ変換する方式に修正した。Resize時も姿勢頂点を再投影する。160×315と315×315で実ピクセル上のモデル縦横比が一致する自動テストを追加した。
- 2026-08-24: 専用GUIのD3Dプレビューへカメラ操作を追加した。左ドラッグでモデル中心を基準にYaw / Pitch回転し、マウスホイールで0.2～5.0倍に拡大縮小する。カメラ状態はビューポートインスタンスごとに保持するため、姿勢適用やボーン選択で視点を失わず、別の編集画面とも混在しない。モデルと骨格を同じ頂点シェーダーで回転し、Pitchは上下反転を避ける範囲へ制限する。回転による投影変化と1.5倍ズームを自動テストへ追加した。
- 2026-08-24: 初回カメラ実装はドラッグ1ピクセルごとにCPUスキニング、三角形展開、GPU頂点バッファ再作成を行ったため、実機で回転が非常に遅かった。姿勢・選択ボーン変更時のシーン再構築とカメラ操作を分離し、ドラッグとホイールでは32バイトのシェーダー定数だけを更新する方式へ変更した。連続200回のマウス移動が2秒以内に終わる退行テストを追加した。
- 2026-08-24: GPUカメラ化後の専用GUIについて、左ドラッグ回転が十分高速で、モデルと骨格の動作にも問題がないことを実機確認した。さらに同一PMXの表示モデルを2個配置し、それぞれ異なる標準姿勢とポーズ参照レイヤーを設定してもデータが混線しないことを確認した。EffectID単位の表示モデルContext、GUIインスタンス単位のカメラ、レイヤー単位の共有メモリ分離を合格とする。
- 2026-08-24: 専用GUIのD3DプレビューへPMX通常テクスチャ表示を追加した。スキニング済み頂点へPMXのUVを保持し、材質単位の描画範囲と通常テクスチャ番号をGPU描画へ渡す。画像はWICで32bit BGRAへ変換してGUIインスタンス所有のD3D11 Texture / SRVとして読み込み、繰返し・線形Samplerを使用する。Pixel Shaderでテクスチャと材質Diffuse RGBA・簡易陰影を乗算し、基本アルファブレンドとほぼ透明な画素の破棄を行う。テクスチャ未指定、不存在、非対応形式、読込失敗時は1×1白画像へフォールバックする。テストPMXで実画像が1枚以上GPUへ生成されたことを自動確認した。スフィアマップ、Toon、エッジは未対応。
- 2026-08-24: テクスチャ表示が若干暗かったため、専用GUIだけにカメラ追従の編集用ヘッドライトを追加した。PMX法線をD3D頂点へ保持し、モデルと同じYaw / Pitchでビュー空間へ回転して、カメラ位置より少し上からモデルへ向かう固定方向とのDiffuseを頂点シェーダーで計算する。初回確認後に少し強め、環境光を0.68、正面光を最大0.42として正面側を最大1.10倍まで明るくし、影側の形状も確認できるようにした。骨格線は照明対象外。AviUtl2側の最終描画と姿勢データには影響しない。
- 2026-08-24: 専用GUIの骨格線クリックによるボーン選択を追加した。左クリックは選択、3ピクセルを超える左ドラッグはカメラ回転として区別する。GPU表示と同じカメラ投影で各親子線分との画面距離を計算し、8ピクセル以内の最寄り線分を選ぶ。線がほぼ同距離で重なる場合はD3D深度が手前の子ボーンを優先する。選択後は骨格の橙色強調と左側ボーン一覧・数値欄を同期する。頂点バッファ生成を`MmdD3DBuffers`へ分離し、Rendererの肥大化も抑えた。
- 2026-08-24: PMXボーン読込を拡張し、変形階層、付与親、付与率、IKターゲット、反復回数、1回の角度上限、IKリンク、リンクごとのXYZ角度制限を保持する。親・付与・IK参照範囲、付与率と角度の有限性、リンク角度の最小最大を検証する。テストモデルは224ボーン、付与変形19、ローカル付与0、IK 4として正常に解析した。
- 2026-08-24: 親子変換、付与変形、CCD IKを`PmxBoneSolver`へ分離して最終姿勢計算へ接続した。ローカル付与は付与元のローカル姿勢を合成し、通常付与は付与元のグローバル差分を対象親座標へ戻して合成する。付与率は移動の倍率とQuaternion回転角の倍率へ適用する。IKは変形階層・ボーン番号順に処理し、各リンクを親座標へ変換して回転し、XYZ制限を適用する。不正PMXによる過大負荷を避けるため実行時反復は最大256回とする。50%回転・移動付与、制限なしIK、0.2ラジアン制限IKの人工テストに合格した。実モデル初期姿勢の最大IK残差はつま先IKの約0.157 MMD単位で、左右対称だった。
- 2026-08-24: 編集操作に依存しないPMXモーフ基盤を追加した。PMXヘッダーのモーフ・材質・剛体Index幅を保持し、グループ、頂点、ボーン、UV、追加UV、材質、フリップ、インパルスの各モーフを最後まで解析する。現在はグループ参照を再帰展開して頂点変位とボーン移動・Quaternion回転を合成し、モーフ適用済み頂点をCPUスキニングへ渡せる。係数配列は表示オブジェクト側で個別所有でき、共有PMXキャッシュへ可変状態を持たせない。テストモデルの78モーフを正常に解析し、人工データでグループ係数から頂点・ボーン・スキニングまで検証した。UV・材質・インパルスは読込と位置整合のみで、適用は未対応。
- 2026-08-24: 専用GUIの選択対象を関節とボーン区間へ分けた。関節付近の左クリックは関節単体、骨格線の左クリックはそのボーン区間と始点関節を選択し、左のボーン一覧は始点関節へ同期する。空白左クリックは選択解除とする。ホバーは黄色、選択は橙色で区別し、ボーン選択時は区間と始点関節の両方を強調する。関節と線が重なる範囲では関節を優先し、同種の対象が重なる場合は手前を優先する。空白左ドラッグと右ドラッグをカメラ回転、ホイールをズームとする。
- 2026-08-24: 選択した関節だけを橙色の低ポリゴン球として表示する。全関節の常時表示はモデルと骨格線を隠し、関節優先判定によってボーン区間も選びにくくなるため採用しない。選択中のボーン区間には、始点関節を四角い底面、先の関節を頂点とする橙色の四角錐を重ね、始点関節球と同時表示する。ボーンのホバー中は四角錐だけを黄色で表示する。四角錐の底面はボーン方向へ直交させ、短いボーンでも見える最小幅と長いボーンで太くなりすぎない最大幅をモデル身長基準で制限する。形状はモデルより前面へ表示しつつ、球・四角錐・骨格線同士では深度を維持する。
- 2026-08-24: ボーン区間のホバー・クリック許容距離を等倍時11ピクセルへ広げ、ズーム倍率の平方根に比例して5～25ピクセルの範囲で変化させる。ズームアウト時に隣接ボーンを巻き込みすぎず、ズームイン時は拡大された骨格を追いやすくする。関節の許容距離は7ピクセルのままとする。
- 2026-08-24: 関節左ドラッグの基本操作を追加した。ドラッグした関節の親ボーンを回転対象とし、画面差分を現在のYaw / Pitch / Zoomからモデル空間へ戻して、親関節から選択関節までの長さを保つ球面上の回転へ変換する。親の親が回転済みでもローカル姿勢へ正しく戻し、ドラッグ中の姿勢をGUI作業用コピーへ同期する。軸制限、伸縮許容、Undo / Redoは未対応。
- 2026-08-24: 空白の左ドラッグをカメラ回転からプレビューの画面内平行移動へ変更した。Yaw / Pitch / Zoomは維持し、Pan X / Yだけをピクセル差分で更新する。GPUカメラ定数へパン量を追加し、モデル、骨格、選択形状、ヒット判定を同じ位置へ移動する。右ドラッグは従来どおりカメラ回転、ホイールはズームとする。
- 2026-08-24: ボーン左ドラッグの基本回転を追加した。選択四角錐の始点関節を中心、先の関節を操作点として、関節ドラッグと同じ球面上の方向変更を選択ボーン自身のローカル回転へ反映する。クリックだけなら選択、ドラッグなら姿勢変更となる。長手方向のひねり、軸制限、角度スナップは未対応。
- 2026-08-24: ボーン・関節ドラッグが非常に重かった原因は、マウス移動1回ごとにCPUスキニング、全三角形展開、モデルGPU頂点バッファ再作成を行っていたことだった。モデル三角形と骨格オーバーレイのGPU資源を分離し、ドラッグ中はボーン変換、骨格線、選択球・四角錐だけを更新する。モデル本体はドラッグ開始時の表示を維持し、マウスを離した時に確定姿勢で1回だけ再スキニングする。ホバー・選択色の変更も骨格専用更新とした。
- 2026-08-24: 骨格専用更新後も遅かった根本原因として、少量の骨格頂点でも毎回D3D11バッファを新規生成・解放していたことと、ドラッグ中にもCCD IKを全反復していたことを確認した。骨格線・形状バッファは2倍単位の容量を保持して`UpdateSubresource`の部分更新で再利用し、ドラッグ中のボーン変換は親子階層と付与変形だけを計算してIKを省略する。マウスを離した時だけ完全なIK、スキニング、モデルバッファ更新を行う。連続100回のボーンドラッグ更新を0.5秒以内とする退行テストへ強化した。
- 2026-08-24: ボーン操作確定時にカメラが姿勢へ追従して見えた原因は、スキニング後の現在頂点からモデル中心、幅、高さ、奥行き半径を毎回再計算していたことだった。プレビュー表示直後または別モデルへの切替時だけ自動フィットし、その中心と投影範囲をGUIインスタンスへ固定する。同一モデルの姿勢更新では固定フレーム内の頂点と骨格だけを再構築し、ユーザーのYaw / Pitch / Zoom / Panと画角を維持する。大きな姿勢移動後も中心と投影範囲が不変であるテストを追加した。
- 2026-08-24: GPUバッファ再利用とIK省略後に、ドラッグ中もモデル本体を追従させる方式を再試験した。テストモデルでは1更新あたり約50～60ms、約18fps相当まで低下した。残る負荷は74,820頂点のCPUスキニング、414,786インデックスの三角形展開、約20MBの頂点転送であり、骨格バッファ生成やIKとは別だった。この方式は不採用としてドラッグ中は骨格だけの更新へ戻した。モデル本体GPUバッファの容量再利用は確定時の更新にも有効なため残す。将来本体をリアルタイム追従させる場合は、インデックス描画、頂点形式の分離、またはGPUスキニングが必要。
- `比較用骨格Xずらし`は`表示モード=両方`のときだけ骨格をモデル横へ離して比較する設定で、モデルの姿勢や頂点位置には影響しない。
- `表示モード`で`標準`（モデルのみ）、`ボーンのみ`（元のモデル位置）、`両方`（モデルとX方向へずらした骨格）を切り替える。骨格は頂点上限を圧迫しないよう親子1組を3頂点の三角形で表し、通常ボーンは水色、IKボーンは黄色にする。
- ボーン表示が出なかった原因は座標ではなく、頂点カラー形式の`draw_poly()`へ画像リソース`nil`を渡したことだった。過去の立方体検証でAviUtl2 v2.1.6aは頂点カラーでも`nil`を拒否すると確認済みだったため、1×1白画像を現在オブジェクトへ設定して`object`リソースを渡すよう修正した。
- 初回のボーン確認版では表示を有効にしてX位置をずらしても骨格が表示されなかった。モデル257,154頂点の後にボーン約2,700頂点を追加したため、`draw_poly()`の累積頂点キュー上限に達した可能性が高い。確認用ボーンをモデルより先に描く順序へ変更した。
- ボーン確認は一旦保留し、画面表示用の設定項目と描画コードを削除した。Debug版のログは`%LOCALAPPDATA%\MMD_Model_Filter\Logs\<PMX名>.bones.log`へUTF-8で出力する。
- 2026-08-24 09:18:53のAviUtl2実機表示でボーンログが更新され、`BoneCount=224`と224行のボーンデータを確認した。先頭は`全ての親`、`センター`、`グルーブ`、`上半身`等、末尾はスカートボーンで、親番号、位置、フラグ、日本語名を正常に取得できている。
- 2026-08-24: `ボーンのみ`で全身の親子骨格が水色、IKが黄色として正しく表示された。ボーン読込・座標変換・親子接続・描画を合格とし、調査用ボーンログのコードと生成済みログを削除した。
- スフィアマップ、Toon、エッジ、テクスチャへのDiffuse RGB乗算は未対応で、初回の静止表示確認後に必要性と実現方法を検討する。
- `.auf2`から独立したメディアオブジェクトを登録するため、`FLAG_VIDEO | FLAG_INPUT`を使用する。
- `draw_poly()`成功後は`false`を返し、空のオブジェクト画像による後続描画を中断する。
- 立方体はXYZ各軸で-150～150の範囲とし、カメラ移動・回転時の面の切替と奥行き遮蔽を確認できる形にする。
- 軸に正対して1面だけに見える状態を避けるため、`draw_poly()`へ渡す頂点自体へX=25度、Y=-35度の回転を適用する。
- 三角形、UV、法線、材質、半透明の基本検証までは裏面カリングを無効にした。現在はカリングを有効にし、外向き面の頂点順を検証する。

## 次の作業

編集GUIで空白左ドラッグのパン、右ドラッグの回転、パン後の選択位置、関節・ボーン左ドラッグの方向と姿勢保存を実機確認する。合格後、ボーン長手方向のひねり操作とX / Y / C / Zによる操作中の軸制限へ進む。固定、左右対称、Undo / Redoは直接操作が成立してから段階的に追加する。モーフ名によるモデル固有割当とフレーム単位の入力データ境界は、基本的な関節・ボーン編集操作の後に再開する。

## 検証結果

- 2026-08-24: 三角形12枚（36頂点）の検証立方体がAviUtl2上で表示された。
- 各面を構成する2三角形の濃淡、赤・黄・マゼンタ面の接続、手前と奥の遮蔽を確認し、三角形描画と基本深度処理を合格とした。
- カメラまたはオブジェクトを回して全6面を確認し、各面が2色の三角形で構成された立方体として見えることを確認した。
- 2026-08-24: 4分割テクスチャが三角形境界をまたいで連続し、立方体の各面に破綻なく貼られることを確認してUV描画を合格とした。
- 2026-08-24: 同一面の2三角形に照明の継ぎ目がなく、隣接面に法線方向に応じた明暗差が出ることを確認して法線・光源描画を合格とした。
- 2026-08-24: 3種類のテクスチャを3回の`draw_poly()`に分けても、全6面の欠落、隙間、深度破綻がないことを確認して材質単位の複数描画を合格とした。
- 2026-08-24: 半透明パネル越しに不透明な立方体と背景が見え、欠落や深度破綻がないことを確認して単一半透明材質の基本描画を合格とした。
- 2026-08-24: 2枚の半透明パネルの重なりに双方の色が残り、その奥の立方体も見えることを確認して複数半透明面の基本合成を合格とした。
- 2026-08-24: カリング有効時も閉じた立方体に面欠けや表示破綻がないことを確認し、三角形の頂点順と裏面カリングを合格とした。
- 2026-08-24: 51,200三角形、153,600頂点、20材質の球体が、輪郭・材質境界・陰影の欠落や破綻なく表示された。カメラ・オブジェクト操作等も「非常に軽い」と確認され、描画負荷を合格とした。
- 2026-08-24: `Model\ふらすこ式風きりたん_ver0.05\ふらすこ式風きりたん_ver0.05.pmx`をテストモデルとして配置した。モデルのReadmeに未改造モデルの再配布禁止条件があるため、`Model/`全体をGit管理対象外とした。
- 同モデルはPMX 2.0、UTF-16、頂点74,820、面インデックス414,786（138,262三角形）、通常テクスチャ18、材質43として整合性検査付き解析器で読込成功した。
- PMX静止表示を材質単位の`draw_poly()`へ接続し、Debug / Release Win64を警告・エラーなしでビルドして実機用プラグインへ配置した。実際の見た目は次回確認する。
- 2026-08-24: 初回実機表示では着物、髪、髪飾りは正常だが、下半分が欠落し、顔も異常だった。欠落境界の`hair_front`終了時点は累積244,752頂点で、次の`bottoms`、`bottomsB`を加えると253,854、294,138頂点になるため、約262,144頂点の描画キュー上限に達した可能性が高い。
- 原因切り分け版では透明度0の材質を除外し、基本の眼・顔・髪・衣装・下半身・手・靴等21材質、合計257,154頂点を優先描画する。眼差分材質も一旦除外し、顔異常との関係を確認する。
- 2026-08-24: 本体材質優先版では顔以外の全身が正常に表示された。したがって下半身欠落はPMX解析ではなく、約26万頂点付近の`draw_poly()`描画上限によるものと判断する。
- 顔は白い眼と不自然な口が残った。眼・顔テクスチャがアトラス形式であり、上下を逆参照した場合の見え方と一致するため、PMXのV座標を`1-V`へ変換する再確認版とした。
- V座標を`1-V`へ反転すると表示が悪化したため、この仮説は不採用として元のV座標へ戻した。顔異常は別要因として調査する。
- 顔、髪、衣装について三角形の幾何法線と頂点法線の内積を比較したところ、いずれも同じ正方向であり、顔材質だけPMX面順が逆という仮説は否定された。顔表面ではなく内側が見えているとの観察に基づき、眼・顔・顔内側の先頭3材質だけ裏面カリングを無効化して再確認する。
- 2026-08-24: 眼・顔・顔内側の先頭3材質だけ裏面カリングを無効化すると、眼、肌、口を含む顔全体が正常表示になった。顔異常の原因はUVやPMX解析ではなく、これらの顔材質に対するAviUtl2側のカリングだったと確定した。
- 2026-08-24: 同じモデル表示オブジェクトを2個配置しても、2人とも欠落なく正常表示された。約26万頂点付近の制限はシーン全体の共有上限ではなく、少なくとも各オブジェクトの描画コールバック単位でリセットされると判断する。複数人表示数は頂点上限よりCPUスキニング、GPU描画負荷、メモリ使用量を実測して決める。
- 2026-08-24: 上方向から見ると頭頂部の髪ポリゴンが欠けて頭皮が露出した。髪本体は10,692三角形中17、右横髪は1,152中9、前髪は3,664中12三角形で幾何法線と頂点法線が逆向きだった。顔と同じ限定対応として、眼・顔に加えて髪本体、アホ毛、横髪、前髪の材質を両面表示にして再確認する。
- 髪関連材質を両面表示にすると頭頂部の欠けが解消したため、この対応を採用する。
- 2026-08-24: テストモデル74,820頂点の変形情報を保持して再解析し、BDEF1 40,781、BDEF2 22,279、BDEF4 11,760、SDEF 0、QDEF 0を確認した。ボーン番号、ウェイト範囲、頂点ごとのウェイト合計検査にも合格した。
- 2026-08-24: 2ボーンの数値テストで、子ボーンをZ軸へ90度回した際に頂点が支点周りの期待座標へ移動すること、および親ボーンの90度回転が子ボーン位置へ正しく伝播することを確認した。
- 2026-08-24: AviUtl2実機で`左腕Z角度`を変更し、左腕が肩を支点に回転して肘・手・指と袖が追従すること、肩周辺に大きな亀裂や法線・面の破綻がないことを確認した。
- 2026-08-24: 任意ボーン名とXYZ回転を実機確認し、比較用骨格のXずらし以外は正常に動作した。Xずらしは用途が伝わるよう`比較用骨格Xずらし`へ改名した。
- 2026-08-24: 2ボーンの姿勢JSONをエンコード後にデコードし、日本語名、移動、Quaternionが維持されることを確認した。モデルの名前付きボーン配列への適用、未知バージョンと壊れたJSONの拒否も合格した。

## 表示モデル・ポーズ編集の責務

- 表情、ポーズ、モーションは時間軸上の外部オブジェクトが所有する。外部指定が現在フレームに存在しない場合、表示モデルは以前の状態を維持せず、その入力を初期値へ戻す。
- セリフ入力からの口パクと自動まばたきは表示モデル内部でモデル固有のモーフへ解決する。乱数や解析の履歴には依存させず、入力データ、モデル識別情報、絶対フレームから同じ結果を再現する。
- 表示モデルはPMX読込・キャッシュ、外部入力の現在フレーム評価、口パク・まばたき、IK・最終姿勢、スキニング、AviUtl2描画を担当する。外部ポーズの編集項目やキーフレームは所有しない。
- ポーズオブジェクトはPMX参照とシリアライズ済み姿勢データを内部に持つ。設定ボタンまたは拡張プラグインから編集し、確定値の正本は常にポーズオブジェクトの姿勢データとする。
- 拡張プラグインはトラックバーと数値入力を中心とし、AviUtl2のメイン画面で黒板、机、床、他モデル、カメラ等を見ながら選択ボーンを調整する。拡張プラグイン上の3Dギズモ操作はメリットを再評価するまで保留する。
- 設定ボタンの専用GUIはPMXモデル、骨格、3Dポリゴンを表示し、モデル単体のポーズを直感的に編集する。編集中は作業用コピーだけを変更してAviUtl2へ反映せず、OKで姿勢データ全体を一括保存し、キャンセルで破棄する。
- 専用GUIを開いている間は拡張プラグイン側の編集を無効にする。初期版はモーダル編集とし、GUIの作業用コピーは共有メモリへ発行しない。ポーズオブジェクトから表示モデルへの確定済み現在値の受け渡しには共有メモリを使用する。

## 全体設計方針

- 汎用3Dではなく、ボーンを利用するMMD専用Filterプラグイン群として設計する。入力プラグインは使用しない。
- PMX / PMDモデルとVMD等のモーションをファイル指定で読み込む。
- MMD側でボーン・モーフ・スキニングを計算して3D頂点を生成し、AviUtl2の`draw_poly()`へ渡す。カメラ、光源、最終3D描画はAviUtl2側を利用する。
- 自前D3Dで最終2D画像を生成する方式は採用しない。D3D等を導入する場合は、ボーン計算やスキニングの高速化用途を中心とする。
- MMD座標をAviUtl2空間へ合わせる内部縮尺を、AviUtl2標準のオブジェクト拡大率とは別に持つ。
- 編集時の負荷対策として描画品質を切り替え可能にする。軽量化では影、エッジ、半透明材質、物理演算等の省略を先に検討し、単純なポリゴン間引きは後回しとする。

## 実装順序

1. PMXのヘッダ、頂点、面、UV、法線、テクスチャ、材質を読み込み、静止モデルを表示する。
2. PMXのインデックスを三角形頂点列へ展開し、テクスチャの異なる材質単位で`draw_poly()`を複数回呼ぶ。
3. 単一ボーン、親子階層、複数ボーンスキニング、IK等の順でボーン変形を実装する。
4. PMX + VMD + AviUtl2の現在フレームから姿勢を計算し、アニメーション表示へ進む。

PMX / PMD / VMDのファイル構造は公開仕様に従う。SDEF、IK、付与変形、モーフ、MMD独自材質、スフィアマップ、エッジ、物理演算、処理順による再現差は必要に応じてMMD実機と比較する。

## 編集データとモデル差への対応

- 大規模なモデル編集機能は持たせず、AviUtl2で必要なボーン割り当て、ポーズ、モーション、表情、口パク、目パチ設定を専用GUIへ集約する。
- `FILTER_ITEM_DATA`は使用しない。データ形式バージョン、モデル識別情報、各種割り当て・定義、フレーム付きボーン／モーフ状態をテキスト入力項目へシリアライズして、AviUtl2オブジェクト自身に保存する。
- 独自キーフレームは全頂点ではなく、フレーム番号、ボーン識別、移動、Quaternion回転、必要に応じてモーフWeightを保持して補間する。
- 標準ボーンと実モデルのボーンを対応付け、自動判定結果を初期値としてユーザーが修正できるようにする。割り当て、表情、ポーズ、モーション、口パク用モーフ等はモデル固有設定として保存する。
- 共通モーションはボーン割り当てを介して再利用する。移動量は体格差の影響があるため、身長、ボーン長、モデル基準サイズ等による相対補正を検討する。

## 外部制御の方針

- 表情、ポーズ、モーションはAviUtl2タイムライン上の別オブジェクトから共有メモリ等でMMD本体へ渡せる構造を目指す。
- 外部オブジェクトはモデル固有のボーン番号やモーフ番号を持たず、種類、名称、強さ、必要なパラメータだけを送る。MMD本体が名称をモデル固有定義へ解決する。
- 外部制御データは特定モデルへ固定せず、同名のモデル固有設定があればそれを優先し、条件が合えば共通データも適用可能にする。
- 複数表情の同時適用を可能にする。口パクは外部から母音と音量を受けてモデル側で補間し、目パチはMMD本体側で乱数間隔の自動処理とする。
- MMD本体での基本合成順は、初期姿勢、基本モーション、追加ポーズ、表情、口パク、目パチ、最終計算、スキニング、描画とする。

## 未決事項

- MMD本体と複数の外部制御オブジェクトを対応付ける対象判定方法。
- 共有メモリの将来拡張時に必要なデータ形式と後方互換規則。
- 同じボーン／モーフを複数入力が操作する場合の優先順位、適用率、上書き／加算、合成順。
- モデル固有データと共通データの解決規則、および体格差に対するモーション補正。
- PMXのIK、SDEF、付与変形、物理演算等を初期版でどこまで対応するか。
- AviUtl2上の実測負荷に基づく軽量表示の具体的な省略範囲。
# 2026-08-24 関節選択を一時停止

- ボーンとの比較検証のため、編集画面のクリック／ホバー対象から関節を一時的に除外した。
- ボーン選択時の根元関節球表示と、左側リストからの選択は維持する。
- 関節がボーンのヒット判定を横取りしないことをスモークテストで確認する。

# 2026-08-24 ボーン操作中のローカル軸制限

- ボーンの左ドラッグ中だけX、YまたはC、Zキーによる軸制限を有効にした。
- 現在のボーン長手方向をローカルYとし、直交するローカルX/Zを安定して生成する。
- X/Zは指定軸まわりの曲げ、Y/Cは横ドラッグによる長手方向のひねりとして扱う。
- キーを離すと従来の自由ドラッグへ戻り、通常時のキー入力には作用しない。

# 2026-08-24 固定視点切替

- プレビューをクリック後、Aで正面／背面、Sで右／左、Dで上／下の固定視点へ切り替える。
- 同じキーを離してもう一度押すと反対側へ切り替え、長押しのキーリピートでは反転しない。
- 固定視点はYaw / Pitchだけを変更し、ユーザーが設定したZoomとPanは維持する。
- ボーンドラッグ中はA / S / Dを無効にし、編集操作へ視点変更を混在させない。

# 2026-08-24 回転角スナップ

- ボーンの左ドラッグ中にGキーを押している間、ドラッグ開始姿勢からの回転差を5度刻みへ吸着させる。
- 自由ドラッグとローカルX / Y(C) / Z軸制限の両方に適用する。
- Gを離すと連続回転へ戻り、通常時には作用しない。現在は回転操作だけなので位置グリッドは未実装。

# 2026-08-24 ボーン固定

- 選択中にLキーを押すと、そのボーンの固定／解除を切り替える。複数ボーンの固定状態を編集セッション内で保持する。
- 固定中のボーンを選択した時だけ、関節球、四角錐、骨格線を暗い赤色で表示し、直接ドラッグによる姿勢変更を受け付けない。Lで解除すると通常の橙色へ戻り操作可能になる。
- 同じLキーの長押しでは連続反転せず、キーを離して再度押した時だけ解除する。
- 現段階は直接操作の固定を対象とする。将来IK編集を追加するとき、固定ボーンをIK拘束へ接続する。

# 2026-08-24 左右対称編集

- 右側編集欄へ「左右対称編集」チェックを追加した。日本語ボーン名中の左／右を交換して反対側ボーンを対応付ける。
- ON中の直接ドラッグでは、操作側のローカル姿勢をモデルX=0面で鏡映し、反対側へ同期する。移動XとQuaternionのY/Zを反転する。
- 数値欄の「選択ボーンへ適用」と「選択ボーンを初期化」も反対側へ同期する。
- 反対側がLキーで固定されている場合、直接ドラッグでは固定側を上書きしない。全初期化は従来どおり全ボーンを対象とする。

# 2026-08-24 Undo / Redo

- 右側編集欄へ「元に戻す」「やり直す」を追加し、Ctrl+Z / Ctrl+Yにも割り当てる。
- ボーンドラッグは最初に動いた時点からボタンを離すまでを1操作として記録する。クリックだけでは履歴を増やさない。
- 数値適用、選択ボーン初期化、全ボーン初期化もそれぞれ1操作として姿勢全体を記録する。
- 最大100操作を保持し、Undo後に新しい編集を行った場合はRedo履歴を破棄する。カメラ、選択、ロック状態は履歴対象外とする。

# 2026-08-24 選択ボーンから先を初期化

- 右側編集欄へ「選択ボーンから先を初期化」を追加した。選択自身と親子階層上の全子孫だけを標準姿勢へ戻す。
- 同じ親から分岐した兄弟ボーン、別ルート、祖先側の姿勢は維持する。
- 左右対称編集ONでは対応する反対側ボーンの枝も同時に初期化し、全体を1回のUndo操作として記録する。

# 2026-08-24 編集GUIの責務分割

- 部分ポーズプリセットは仕様検討中のため実装を保留し、現在の編集GUI機能をここで固定する。
- 471行だったEditorから静的VCL配置を`MmdPoseEditorLayout`へ分離し、Editor本体はモデル、姿勢、履歴、イベント統括だけを担当する。
- 448行だったRendererからデバイス、SwapChain、固定描画状態、サイズ依存RenderTargetを`MmdD3DDevice`へ分離する。
- 521行だったViewportからVCL HandleとRendererの寿命、Paint、Resize、シーン更新を`MmdD3DViewportSurface`へ分離し、Viewport本体は入力と姿勢編集に限定する。
- 左右鏡映はD3D非依存なので`Editor`直下へ移し、Model / Pose両プロジェクトの明示参照と検索パスを揃える。
- 公開APIには呼び出し側から見た責務を記載し、GPUバッファと関節選択の古い仕様コメントを現在の実装へ合わせた。

# 2026-08-24 一時モーフ動作確認GUI

- 表情、まばたき、口パクの成立を先行確認するため、共通ポーズ編集GUIの左下へ「モーフ動作確認（保存されません）」を追加した。AviUtl2のFilter設定項目と姿勢JSONは増やさない。
- PMX内の全モーフを名前と種別付きで一覧表示し、選択モーフを0～100%で調整する。複数モーフの確認値は画面を閉じるまで保持し、「全て0%」で一括解除する。
- 頂点、ボーン、グループ、フリップモーフをD3Dプレビューへ即時反映する。UV、追加UV、材質、インパルスは一覧へ未対応と表示し、操作を無効にする。確認値はOK時にも保存しない。
- プレビュー変形は編集姿勢の作業用コピーへボーンモーフを合成し、モーフ済み頂点位置をCPUスキニングへ渡す。ボーン操作中の骨格にもボーンモーフを合成する。
- 人工データでグループモーフから頂点・ボーン変形を展開し、プレビュー変形経路のスキニング結果まで合格した。実PMXは現在`Model`フォルダに存在しないため、モデル固有の「まばたき」「あ」「い」「う」「え」「お」等の見た目確認は未実施。
- Debug / Release Win64のModel / Pose両プロジェクトを警告・エラーなしでコンパイルした。AviUtl2起動中で既存`.auf2`がロックされているため、今回の実機用ファイルへの上書き配置はAviUtl2終了後に行う。
- 初回実機ではモーフ確認Panelのコンストラクタ中に、Parent未設定のListBoxから`ItemIndex`を取得してHandle生成を先行させ、`InvalidControlOperation`になった。コンストラクタはHandle不要の初期値設定だけとし、Parent設定後の`SetModel`で一覧と操作状態を同期するよう修正した。親未設定で生成後にFormへ接続する再現テストを追加して合格した。
- 実モデル78モーフをGUIと同じ変形経路へ個別に100%適用し、対応する頂点モーフ68件すべてでスキニング後の頂点変化を確認した。PanelのTrackBarをテストから100%へ変更して係数配列が1.0になることも確認した。率変更時はD3Dシーン再構築後にControlを即時`Update`し、TrackBar操作中も遅延Paint待ちにしない。
- 一時確認値は現時点で専用D3Dプレビューだけへ渡しており、AviUtl2本画面のモデル描画には未接続である。本画面でも同時確認する場合は、一時係数のスレッド安全な共有、再描画通知、画面を閉じた時の確実な解除を追加する。
- 2026-08-24: 修正版GUIでモーフ率に応じて専用D3Dプレビューが変化することを実機確認した。最初に変化しなかった対象は、見た目へ反映されないモーフを操作していた可能性が高い。PMX一覧に存在しても、未対応のUV・材質・インパルス系、または現在描画していない差分材質だけを対象とする場合は外観が変化しないため、一覧の未対応表示と実際の対象部位を区別する。

# 2026-08-24 破棄前提のフレームモーション再生テスト

- `Source\Plugin\Model\Temporary\MMD_Model_TemporaryFrameMotion.pas`へ、AviUtl2の絶対タイムラインフレームだけからボーン姿勢とモーフ率を生成する一時テストを隔離した。途中フレームへ移動しても履歴に依存せず同じ姿勢を再現する。
- 150フレーム周期で上半身を左右へ最大4度、頭を前後へ最大3度揺らす。18フレーム単位で「あ・い・う・え・お」を順番に0～85%で開閉し、90フレームごとの70～78フレーム区間でまばたき、にこり／笑いを0～30%で緩く変化させる。モデルに名前がない要素はその項目だけ無視する。
- 合成順は標準姿勢、外部ポーズ、一時フレームモーション、モーフ展開、最終ボーン計算、CPUスキニング、AviUtl2描画とする。これによりモデル表示オブジェクトを再生するだけで、本画面の姿勢と表情がフレームごとに変わる。
- 人工モデルで同一フレームの再現性、別フレームの姿勢差、母音切替、フレーム74のまばたきを確認した。実モデルのフレーム9でも名前解決に成功し、スキニング後の最大頂点差約0.271 MMD単位を確認した。Model Debug / Release Win64は警告・エラーなし。
- テスト停止だけなら同ユニットの`TEMPORARY_FRAME_MOTION_ENABLED`をFalseにする。完全撤去時はModel Filterから同ユニットのusesと`ApplyTemporaryFrameMotion`呼出しを削除し、DPR / DPROJの明示参照と`Temporary`フォルダを削除する。汎用のモーフ適用済み頂点をスキニングする描画経路は、今後の表情・口パク・まばたき実装にも必要なため残す。
- 2026-08-24: AviUtl2でモデル表示を再生し、絶対フレームに応じてボーン姿勢、母音口形、表情、まばたきが変化することを実機確認した。再生時の追加負荷は体感上ほぼ0だった。フレーム単位のモーション生成、モーフ合成、CPUスキニング、AviUtl2描画までの基本経路を合格とする。
- 2026-08-24: D3D編集プレビューでは見えるきりたんぽと帯・背面装飾の一部がAviUtl2本画面で欠けることを確認した。原因はモーフではなく、約262,144展開頂点の上限対策としてRendererが21材質だけを固定選別していることだった。現在選択済みは257,154頂点で、`weapon`材質9,216頂点、`bag`33,564頂点、`ribbon`23,352頂点はいずれも対象外である。`weapon`を単純追加しただけでも266,370頂点となり上限を超えるため、単純なホワイトリスト追加では解決しない。

# 2026-08-24 D3D本体メッシュ追従テスト

- 実機確認を終えた一時フレームモーションは、Model Filterの描画経路、DPR / DPROJ、専用ソース、専用テストから完全に撤去した。AviUtl2本画面は外部ポーズだけを反映する通常表示へ戻した。汎用モーフ変形と設定画面のモーフ確認GUIは残す。
- 設定画面でボーンを左ドラッグした各MouseMoveごとに、骨格だけでなく本体メッシュも再変形し、D3D頂点バッファを更新して即時描画する試験を有効にした。ボーン姿勢計算、モーフ展開、CPUスキニング、三角形展開、GPU転送、Presentまでを実際の操作頻度で通す。
- 試験スイッチは`Source\Lib\MMD\Editor\D3D\Temporary\MmdD3DLiveDragTest.pas`へ隔離した。`TEMPORARY_LIVE_MODEL_DRAG_TEST = False`にすれば、ドラッグ中は骨格だけを更新し、ボタンを離した時に本体を更新する従来動作へ戻る。
- 100回の連続MouseMoveを含むD3Dスモークテストを実モデルで実行し合格した。試験時は本体再構築を含むためハング検知上限を10秒、従来動作では500msとする。Model / PoseのDebug / Release Win64はいずれも警告・エラーなし。
- AviUtl2が起動中のため、ビルド済み`.auf2`の実機配置は終了後に行う。
- 2026-08-24: 設定画面でボーンを操作した際、本体メッシュがドラッグへ連続追従することを実機確認した。体感負荷はあまりなく、現在のモデル規模では毎MouseMoveの全メッシュ再変形とD3D更新も実用候補になる。恒久機能にするか、更新間引き、より重いモデルでの上限、CPUスキニングの維持／GPU化については今後検討する。

# 2026-08-24 低ポリゴンモデルの材質欠落修正

- 欠落は約262,144頂点の上限だけでなく、ふらすこ式きりたん用の固定材質番号21件を全モデルへ適用していたことが原因だった。約24,474展開頂点の走るキリンタウロスでも材質5、7、10の計426頂点が除外されていた。
- 骨格分を含む全材質の展開頂点数が262,144以下なら、材質番号に関係なくPMXの全材質を元の順序で描画する。上限を超えるモデルだけ、従来のきりたん主要材質優先順へフォールバックする。
- 頂点カラー材質にもAviUtl2が要求する1x1白の`object`画像資源を渡し、テクスチャを持たない材質が`nil`資源で拒否される経路も修正した。
- 材質番号5、7、10を含む低ポリゴン人工モデルで全材質が選択され、大型モデルで従来の優先順へ戻ることを自動テストした。Model Debug / Release Win64は警告・エラーなしでビルドし、Debug版を実機用`.auf2`へ配置した。
- 実機でずんだもんは大部分が戻ったが一部が欠けた。設定画面D3Dがカリングなしなのに対し、全材質化後のAviUtl2経路はPMXの片面指定を使っており、左右反転で頂点順が逆の部位を消す可能性があった。AviUtl2側も全材質を両面描画へ統一した。Debug / Releaseは警告・エラーなしだが、AviUtl2起動中のため実機配置は終了後に行う。
- 2026-08-24: 両面描画版で、ずんだもんの左右の手足、衣装、靴と、低ポリゴンの走るキリンタウロスが欠落なく表示されることを実機確認した。262,144頂点以下のモデルに対する「全材質＋両面描画」を合格とする。

# 2026-08-25 ポーズ編集GUIの高DPI対応

- ポーズ編集フォームはDFMを使わず`CreateNew`後に全Controlの寸法を96 DPI基準値で設定していたため、200%環境ではフォントだけが大きくなり、左右欄、数値入力、長いボタン、モーフ確認欄が重なったり切れたりしていた。
- 全Controlの生成と配置が完了した時点で、完成したフォーム階層を96 DPIから現在の`Screen.PixelsPerInch`へ一度だけスケーリングするようにした。フォーム、左右パネル、D3D表示領域、入力欄、ボタン、モーフ確認欄が同じ倍率で拡大される。
- `CreateNew`直後のVCL標準フォントはすでに現在DPI用の高さを持つため、一括スケーリング前の高さを保存して復元する。これにより200%でフォーム寸法だけを拡大し、文字がさらに2倍になる二重DPI補正を防ぐ。フォームのDPI状態は更新するので、異なるDPIのモニターへ移動した後のVCL自動補正は維持する。
- 96 DPIと200%相当の192 DPIについて、入力欄幅、モーフ欄高さ、ボタン文字の収まりを自動テストし合格した。Model / Pose両プロジェクトのDebug / Release Win64もビルド成功。AviUtl2起動中のため実機用`.auf2`への配置は終了後に行う。

# 2026-08-25 参照画像用Ctrl+C

- ポーズ編集フォームで`Ctrl+C`を押すと、現在のD3D表示領域と同じピクセル寸法・カメラ・背景・モデル姿勢のBitmapをWindows Clipboardへコピーする。数値入力欄にフォーカスがある場合は通常の文字コピーを優先する。
- コピー時だけPresent前のBackBufferへモデル本体を描き、骨格線、関節球、選択形状を除外してD3D11 staging textureからCPUへ読み戻す。コピー後は通常の骨格付きフレームを即時描画し直すため、編集画面と姿勢は変化しない。
- Clipboard依存は`MmdPoseImageClipboard`へ隔離し、RendererとViewportは汎用的なBitmap取得だけを公開した。今後の`Ctrl+V`参照画像保持や等倍重ね合わせは同じBitmap経路へ追加できる。
- 実モデルのD3Dスモークテストで画像取得とViewport寸法の一致を確認した。Model / Pose両プロジェクトのDebug / Release Win64はエラーなしでビルド成功。AviUtl2起動中のため実機用`.auf2`への配置は終了後に行う。
- 初回実機ではD3D行とVCL `ScanLine`の上下を追加反転したため、Clipboard画像が上下逆になった。両者を同じ行番号でコピーするよう修正した。

# 2026-08-25 参照画像用Ctrl+V

- ポーズ編集フォームで`Ctrl+V`を押すと、ClipboardのBitmap / DIB / DIBV5画像をフォーム内の独立Bitmapとして保持し、現在のD3D表示へ半透明で重ねる。貼り直すと前の参照画像を置換し、フォームを閉じると破棄する。数値入力欄では通常の文字貼り付けを優先する。
- 同じViewportから`Ctrl+C`した画像は同じ表示寸法で重なる。異なる寸法ではViewport高さを基準に縦横比を保って拡大縮小し、横方向を中央へ配置する。MMD投影上のモデル高さもViewport高さに比例するため、参照モデルの大きさを現在モデルへ合わせやすい。
- 実モデルのD3Dスモークテストで、元Bitmapを解放した後も参照画像が内部保持され、描画を継続できることを確認した。

# 2026-08-25 参照画像への概形合わせ

- 右側編集欄へ「画像へ概形合わせ」を追加した。参照画像を貼り付けた後だけ有効になり、実行時に現在モデルの骨格なし画像と参照画像を比較して主要ボーンを段階探索する。貼り付けだけでは姿勢を変更しない。
- 対象は上半身、上半身2、首、頭、左右の肩・腕・ひじ・足・ひざの限定軸とし、30度、15度、7.5度の順で参照画像との差が減る方向だけを採用する。Lキーで固定したボーンは自動調整対象から除外する。
- 比較は暗いプレビュー背景からの前景マスク差を優先し、前景同士ではRGB差も加える。同じViewportから`Ctrl+C`して加工した、同じアングル・背景・ほぼ同じ外観の画像を主対象とする。外部画像や背景が変わった画像では手動仕上げを前提とする。
- 自動調整結果全体を1回のUndo操作として記録する。画像差を改善できない場合は姿勢とUndo履歴を変更しない。探索中だけGPUからCPUへの画像取得とモデル再変形を繰り返し、通常の編集・再生時には追加負荷を持たない。
- 実モデルで左右の腕を45度変えた参照画像を生成し、標準姿勢から画像差が減少して腕ボーンが自動で変化することをD3Dスモークテストで確認した。
- Model / Pose両プロジェクトのDebug / Release Win64と200% DPIテストに合格し、AviUtl2終了中に最新Release DLLを実機用`.auf2`へ配置した。

# 2026-08-25 足ボーン表示とFK編集の修正

- 参照画像への概形合わせは追加調整をいったん保留する。貼り付け、手動編集、現状の概形合わせコードは残すが、この修正では変更しない。
- 本体メッシュはIK適用後の最終ボーン変換で描画していた一方、選択・ホバー・ドラッグ時の骨格だけ反復IKを省略していた。足IKの影響が大きい足～ひざ～足首だけ骨格が本体から大きくずれ、つま先系は正常に見えていた。
- 骨格オーバーレイも本体と同じIK適用後の最終変換を使うよう統一した。実モデル224ボーンについて、骨格表示用と本体スキニング用の全ボーン位置が一致することを確認した。
- 足・ひざなどIKリンクボーンへ明示的な移動または回転が設定された場合、その入力をIKで直後に上書きせず、該当IKチェーンだけFKとして計算する。全リンクが初期姿勢なら従来どおり足IKを適用し、IKコントローラーとつま先系の動作を維持する。
- 人工IKモデルで通常IK、角度制限、明示FK切替を確認した。実モデルで右ひざを10度回し、右足首の最終変換と実際のスキニング頂点がともに移動することを確認した。D3DスモークテストとModel / Pose両プロジェクトのDebug / Release Win64ビルドに合格した。

# 2026-08-26 AI MIRAIポーズプロバイダー初版

- Model / Poseプラグインからバージョン1のC ABIを公開し、外部AIの入口はAI MIRAIのNamed Pipe `Aul2MIRAI.v1`へ統一した。MMD側に別Pipeは設けない。
- `get_model_schema`はモデル名・パス・ハッシュ、全ボーンのindex・名前・親・初期位置・変形階層・フラグ、IKターゲット・反復回数・角度・リンク・制限を返す。実モデルで224ボーン、IK 4本を確認した。
- `preview_pose`は`replace` / `merge`、移動量、QuaternionまたはEuler度数を受け、既存の姿勢形式へ正規化する。重複・未知ボーン・不正値を拒否し、指定ボーンの最終ワールド位置も返す。
- 実モデルの左腕へEuler `[0,0,30]`を与え、Quaternionへの正規化と最終位置を確認した。存在しないボーンは`bone_not_found`で拒否された。
- Model / Pose / Aul2MIRAIのWin64 Releaseビルドは警告・エラーなし。実機用配置後、Named Pipeから実モデル224ボーン・IK 4本の取得、奥行き回転を含む3ボーンの非破壊プレビュー、未知ボーン拒否、一時的な頭ボーン変更と適用後照合に成功した。試験後は元姿勢へ戻し、`state_token`が試験前と一致することを確認した。
- AviUtl2設定値APIの16,384文字上限を超える全身姿勢と、変更直後のフレーム取得を確実に最新描画へ同期する仕組みは次段階で扱う。
